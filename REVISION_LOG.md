# Early Bird — Revision Log

Moved from EB_DESIGN.md on 2026-04-11. This file is not read at session start. Reference it only when explicitly asked to review revision history.

### Session log

- 2026-04-10 — 01. landing-buyer — restructured above-fold to lead with sign-in form, added top-right "Dealer? →" link, added FAQ + About sections, replaced footer `border-t` with `bg-base-200` panel treatment (T10), swapped hero copy to "Shop the drop" framing (T7).
- 2026-04-10 — 02. landing-dealer — restructured above-fold to lead with sign-in form, added top-right "Buyer? →" link, hero/cards/FAQ/About all hammer free-to-use + no-cut + direct-transaction, added "Dealers shop too" value prop card + FAQ item acknowledging dealer-as-buyer, replaced footer `border-t` with `bg-base-200` panel (T10), swapped "pre-market your booth" for "Sell the drop. Sell before sunrise." framing (T7).
- 2026-04-10 — 03. onboarding — dropped Welcome/Step-1-of-1/Heads-up clutter, swapped Upload Photo for Take Selfie, made display name free-form, added Follow Markets + Notifications sections, fixed verified pill (outline + padding, T2/T3), replaced footer `border-t` with `bg-base-200` panel (T10), swapped "Get set up to pre-buy" for "Get set up to shop the drop" (T7); also fixed EB_DESIGN.md "The Idea", "Who It's For" buyer line, and "Onboarding" bullet inline (T7 + 03.6).
- 2026-04-10 — 04. buy-feed — enlarged dealer avatars (w-6→w-10), wired EARLY BIRD logo to `home-buyer.html` (T8), replaced footer `border-t` with `bg-base-200` panel (T10); also added Navigation post-login flow + new Home Lobby (Buyer) screen + `home-buyer.html` to Phase 1 file list in EB_DESIGN.md (04.3/T8). HELD/SOLD T2/T3 restyle reverted same day to stay consistent with un-revised screens — T2/T3 will be re-applied across all screens together.
- 2026-04-10 — design decision (cross-screen) — buy feed has no filters in v1: removed entire filter bar from `buy-feed.html` (ALL/SAVED toggle + Dealer + Category dropdowns); SAVED is redundant because Watching is the saved-and-inquired view. Watching tab is scoped to the current market only — removed cross-market Folk Art Weather Vane item from `watching.html` and updated count from 8 to 7. Spec updated: Buy Tab and Watching Tab sections in EB_DESIGN.md rewritten. Note: `watching.html` still pending its full 05 revision pass (header/market context).
- 2026-04-10 — partial 05. watching + cross-screen nav update — wired EARLY BIRD logo on `watching.html` to `home-buyer.html` (T8), replaced "Watching / 7 items you care about" header with eyebrow + market context line ("Downtown Modernism · Apr 26") so the screen tells you which market it's scoped to; bottom-nav Watching link now shows the count "Watching (7)" across all 12 screens with a bottom nav (buyer + dealer); `account-buyer.html` Watching stat updated 8 → 7 to match. Full 05 revision still queued for its own session.
- 2026-04-10 — sell-add-item — removed Category form field (and the 10-option select) since v1 captures no category data and offers no category filter. Open Question 1 (Categories) marked deferred to v2 in EB_DESIGN.md.
- 2026-04-10 — 05. watching — completed revision pass: hearts neutralized (text-error → text-base-content, 05.4 canonical), inquiry-sent rows now show the actual buyer message inline (05.2), bottom nav switched to bg-base-200 panel (T10). Deferred 05.1/05.3 (T2/T3 on PRICE DROP/HELD/SOLD) to a cross-screen pass. Flagged buy-feed.html as also needing red→neutral hearts.
- 2026-04-10 — 06. item-detail-buyer SPLIT — clean buyer-view state revised in place; inquiry compose extracted into new item-detail-buyer-inquiry.html as a raw Tailwind bottom drawer with backdrop + drag handle (NOT DaisyUI modal, T12). Applied T1/T4/T5/T7/T8/T10 + neutralized hearts. Added new file to review.html WIREFRAMES (count N → N+1). Deferred T2/T3 on badges to cross-screen pass.
- 2026-04-10 — new cross-cutting theme T13 added to PHASE_1_REVIEW_NOTES.md — Market status indicator: replace the green "LIVE" text pill with a lightning-bolt circle, and add a calendar circle for upcoming (not-yet-live) markets. Deferred to a cross-screen pass. Current instances flagged across 7 wireframes (buy-feed, item-detail-buyer, item-detail-buyer-inquiry, item-detail-dealer-browsing, sell-market-picker, sell-add-item, sell-booth-active).
- 2026-04-10 — 07. item-detail-dealer-own — added helper line under LIVE/HOLD/SOLD tabs clarifying that the dealer should mark SOLD the moment the item actually sells (07.3 copy-clarity); switched bottom nav from `bg-base-100 border-t border-base-300` to `bg-base-200` panel (T10). T4 back affordance (07.1) already addressed in 6363d2c via overlaid `← Back` text button on hero. T5 header label (07.4) N/A — no header bar exists on this screen. T8 logo routing (07.5) N/A — no logo element exists on this screen (edge-to-edge hero, same reasoning as 06). T1 N/A — no sticky CTA stacked above bottom nav. Deferred 07.2 and 07.3 color-half to the cross-screen T2/T3 pass; deferred T13 LIVE-pill replacement to the cross-screen pass.
- 2026-04-10 — 07. item-detail-dealer-own follow-up — pulled 07.3 color-half and the 07.2 inquiries-pill fix OUT of the T2/T3 deferral because both controls are unique to this screen (no cross-screen consistency to protect). LIVE/HOLD/SOLD selector rebuilt from `tabs tabs-boxed` (which rendered the active tab in loud primary purple) to a `join` of `btn-sm` buttons with `btn-neutral` for the active state, matching the T6 segmented-control pattern used elsewhere in the app. "3 inquiries" badge dropped entirely; count folded into the section label as "Inquiries (3)" to match the `Watching (7)` pattern in the bottom nav. 07.2 market-LIVE pill in the "Listed In" card is still deferred — that one IS cross-screen.
- 2026-04-10 — spec addition — new product concept: per-inquiry Hold/Sell buttons on each inquiry card in `item-detail-dealer-own`, with one-way transactional SMS receipts to the affected buyers. Full spec added to `## Communication: One-Touch Inquiry Handoff` → `### Per-Inquiry Transactional Receipts (Carve-Out)` including per-inquiry action behavior, SMS templates, the governance rule coupling per-inquiry actions to the global Live/Hold/Sold status (per-inquiry sets global; global change while a per-inquiry commitment is active triggers a confirm drawer), and the walk-up fallback. New follow-up checklist item added for the dedicated future session that will build it (wireframe revision + confirm-drawer split + review.html update + CLAUDE.md carve-out amendment). No wireframe changes this session.
- 2026-04-10 — 08. item-detail-dealer-browsing — T5 state labels scrubbed: `<title>` tag now reads "Brass Desk Lamp" (was "Item Detail (Dealer Browsing)"), and the `<!-- dealer browsing another dealer's item -->` code comment renamed to `<!-- Buyer-context alert -->`; bottom nav switched from `bg-base-100` to `bg-base-200` so the CTA+nav sticky block is a single unified panel (T10). "Art deco" sub-style fix N/A — no category section exists in this file. T4 back affordance already addressed in 6363d2c (overlaid `← Back` text button on hero). T8 N/A — no logo element (edge-to-edge hero, same as 06/07). T1 N/A — CTA and bottom nav already live inside a single fixed `bg-base-200` wrapper; no gap to fix. Deferred: price-drop pill T2/T3 to cross-screen pass (same call as 05/06/07); LIVE market-status pill T13 to cross-screen pass.
- 2026-04-10 — 09. sell-booth-setup — removed "Row / Area" select and "Landmark Hint" input (booth number alone); rebuilt Payment section as a 5-checkbox method list (Cash, Venmo, Zelle, Apple Pay, Card) with the Venmo @handle and Zelle phone/email inputs deleted, section retitled "Accepted Payment", and helper copy rewritten to spell out the in-person-after-confirmation policy (T11 canonical fix on this screen). T4 back affordance: tiny `btn-circle btn-ghost` `←` replaced with a labeled `← Back` text button in the header bar (form-screen variant of T4, not the hero-overlay pattern from 07/08). T10: nav switched `bg-base-100` → `bg-base-200` so the CTA+nav sticky block is a single unified panel. User feedback also addressed: "Switch" button removed from the Market card (back arrow → home-dealer covers it), "Booth Location" section header dropped (only one field — section header collapsed to "Booth Number"). EB_DESIGN.md: T11 policy rewritten in Sell Tab → Setup (line ~134) and clarified in Account → Dealer (line ~144); Tech Stack → Notifications grep-clean for handle refs; new Home Lobby (Dealer) section added to The Screens parallel to Home Lobby (Buyer); `home-dealer.html` added to Phase 1 wireframes file list (Navigation post-login flow text already covered both roles from the 04 session). New OQ7 added: dealer vetting + fraud / customer-complaint handling (the explicit no-payment-middleman stance leaves an unsolved adjacent question — needs an answer before any dealer-facing launch). T5 N/A — `<title>` tag and header text both already say "Booth Setup", no debug/state strings present. T7 N/A — grep clean, no pre-buy/preview language. T8 N/A — no EARLY BIRD logo element exists on this screen (header is back + title only); same call as 06/07/08, do not invent a logo to satisfy T8. T1 N/A — sticky CTA and bottom nav already live inside a single fixed `bg-base-200` wrapper, no gap to fix. T2/T3 N/A — no badges/pills on this screen at all. T13 N/A — no LIVE pill on this screen. The cross-screen PRICE DROP / DROPPED pill removal (commit 7976819) did not touch 09 — this screen had no PRICE DROP pill. home-dealer.html intentionally NOT built in this session (note 09.2 is its own checklist item).
- 2026-04-10 — 10. sell-booth-active — follow-up pass driven by direct user feedback after shipping the first revision: hierarchy was still flat. Cuts: (a) dropped the entire ALL/LIVE/HELD/SOLD `join` filter row — held/sold items are now ordered to the bottom of the list instead of being a filter dimension, which removes a whole row of UI without losing the affordance; (b) dropped the per-item LIVE outline badges (LIVE is the default state, default doesn't need a label — only HELD and SOLD render a state badge now); (c) dropped the per-item Edit button on every row (dealers tap into an item to edit it from the detail screen); (d) dropped the "asks" pill on Items 1 and 2 and folded the count into the watcher data line as plain text ("· 12 watchers · 3 inquiries", "· 4 watchers · 1 inquiry") with the same `text-[10px] text-base-content/60` treatment as the watcher count; (e) dropped the booth-number subtitle line in the header ("Booth 42 · Tap to switch market") because it's the dealer's own booth — they don't need to be told what booth they're at; (f) un-buttonized the market context line (was a `<button>` with switch-market intent) into a plain `<div class="flex items-center justify-between">` carrying just the market name and the LIVE pill — the switch-market affordance is owned by home-dealer per the 09 session decision; (g) `+ Add` button moved out of a filler-flex row and into a left-aligned slot (the action bar `<div>` now contains only the button). Items reordered in markup to put live items first (Walnut Credenza → Oak Rolltop Desk → Pair of Cane Chairs), then HELD (Teak Nesting Tables), then SOLD (Enamel Kitchen Table). Item dev comments preserve their dummy-data identity numbers (Item 1, 2, 3, 4, 5) — those refer to the underlying data row, not the visual position. Per-item action button counts after Edit removal: live items have 2 buttons (Hold + Drop Price for plain live; Hold + Mark Sold for live-already-dropped); held has 2 (Release + Mark Sold); sold has 0. Hold/Release toggle stays in slot 1 (was slot 2 in the previous pass) since slot 1 is now the leftmost and there's no Edit ahead of it. Header LIVE pill on the market context line is preserved per T13 (deferred). FAB (`+`) and bottom nav unchanged. The redundant `+ Add` action-bar button + `+` FAB pair is preserved — no instruction to remove either, both stay. Original first-pass revision below stays in the log because it landed and shipped (576fc84) — this is a follow-up commit on the same screen, not a redo.

- 2026-04-10 — 10. sell-booth-active (first pass, before user feedback) — slowed down per 10.5 before touching markup. Re-examined screen purpose (dealer's real-time board during a live market: temperature stats + per-item state + quick actions for walk-up sales and inquiry follow-up) and concluded the visual-noise complaint was driven by per-item LIVE/HELD/SOLD/asks badges all using filled DaisyUI semantic colors (`badge-success`, `badge-warning`, `badge-neutral`) which collectively washed out the actual button-level hierarchy. T6 (10.1): filter row rebuilt — `tabs tabs-boxed` with four `<a role="tab">` children replaced with a `join` of four `btn-sm join-item` buttons (active = `btn-neutral`, inactive = `btn-outline`), matching the height of the neighboring `+ Add` `btn-sm` and using the same primitive 07's status selector landed on. T2/T3 (10.2): per-item LIVE/HELD/SOLD badges and "asks" badges all collapsed to `badge badge-outline` (dropped `badge-sm`, `badge-success`/`badge-warning`/`badge-neutral`, `font-bold`, the colored dot span on LIVE badges, and the `gap-1` that the dot needed) — 7 outline badges total. The header market-switcher LIVE pill at line 47 is intentionally NOT touched: that one is a market status indicator and is governed by T13, which is explicitly deferred to a cross-screen pass and explicitly forbidden inside a single-screen revision session. 10.3 (merge Hold/Release): structurally already a single slot per row (no item shows both Hold and Release), but the toggle's screen position was inconsistent — it sat in slot 3 on Items 1 & 4 and slot 2 on Items 2 & 5. Reordered Items 1 & 4 buttons to put Hold in slot 2, so the toggle slot is now positionally consistent across all rows that have it (Item 1: Edit · Hold · Drop Price; Item 2: Edit · Release · Mark Sold; Item 4: Edit · Hold · Drop Price; Item 5: Edit · Hold · Mark Sold). 10.4 (drop the DROPPED pill): already handled cross-screen in 7976819 — confirmed via grep that the only remaining `dropped` reference is the `<!-- Item 5 - Live, price dropped -->` HTML comment, which 7976819's commit body explicitly preserved as describing the underlying data state. T10 (10.6, canonical source on this screen): bottom nav switched from `bg-base-100 border-t border-base-300` to `bg-base-200`, matching the panel treatment 04/05/06/07/08/09 all converged on. T7 (10.7) N/A: grepped `pre-buy|preview|pre-market` (case-insensitive), zero matches on this screen. T8 (10.8): EARLY BIRD logo wrapped in `<a href="home-dealer.html">` (file doesn't exist yet — `home-dealer` is its own checklist item, link will resolve once that session ships). T1 N/A — no stacked sticky CTA bar above the bottom nav on this screen; the FAB is a separately positioned `bottom-20` button, not part of a sticky CTA stack, so there is no gap-glitch to fix. T4 N/A — this is a top-level Sell-tab landing screen with no back button at all. T13 deferred — see header LIVE pill note above. Items NOT touched per the rules: stats block, header market switcher, FAB, Mark Sold action availability per item state (preserved as the wireframe specced — Items 1/4 have Drop Price not Mark Sold; Item 5 has Mark Sold not Drop Price; that state-aware variation is intentional spec, not an inconsistency to fix).
- 2026-04-10 — 10. sell-booth-active third pass — direct user feedback on the HELD row after the follow-up landed: "it's crazy to have the 'held' thing above the title and the button to signify something as 'held' is below ... Merge those things into one design component ... have them below ... pockmarked." Strategic restructuring of state indication on Items 2 (HELD) and 3 (SOLD): standalone state badges above the title DELETED on both rows; the state signal is merged with the state-toggle action into a single component in the button row below the title. Item 2 (Teak Nesting Tables, HELD): `<div class="flex items-center gap-2 mb-1"><div class="badge badge-outline">HELD</div></div>` above title REMOVED; `btn btn-xs btn-outline` "Release" in the action row REPLACED with `btn btn-xs btn-neutral` labeled "HELD" — the filled `btn-neutral` treatment carries the active-state signal that the outline badge used to carry, the label identifies the state, and the element is still tappable to toggle off (same Release interaction semantically, no functional loss). Second slot stays `btn btn-xs btn-outline` "Mark Sold". Item 3 (Enamel Kitchen Table, SOLD): `<div class="flex items-center gap-2 mb-1"><div class="badge badge-outline">SOLD</div></div>` above title REMOVED; a new `<div class="flex gap-2 mt-2">` slot is added below the price/data line containing a single `btn btn-xs btn-neutral` with HTML `disabled` attribute labeled "SOLD" — this places the SOLD indicator in the same vertical slot as HELD's component for visual parallelism across rows, and `disabled` signals terminal state (not a toggle, no action to take). Live items (1, 4, 5) intentionally carry NO state badge and keep their existing outline-button pairs — default state doesn't need a label, and the outline-vs-filled treatment now reads as "special state = filled neutral" vs "live state = outline actions only." Emerging row-layout rule: the LEFTMOST button in the action row is the row's primary state control — live rows = `Hold` (outline, tap to hold), HELD rows = `HELD` (filled neutral, tap to release), SOLD rows = `SOLD` (filled neutral disabled, terminal). Secondary actions (Drop Price, Mark Sold) sit to the right; SOLD rows omit the secondary action slot entirely. First-pass and follow-up pass rationale unchanged. T1/T4/T5/T6/T7/T8/T10/T13 all unchanged from the follow-up pass — this third pass touches only the badge→button merge on rows 2 and 3. CLAUDE.md "no new HTML elements" interpretation: the SOLD button-row addition is a relocation/transformation of the existing SOLD state signal from above the title to below it — the text "SOLD" and the semantic payload are unchanged, the rule protects against inventing new functionality/sections and this is a design restructuring within the row's existing footprint. No inline styles introduced. No class renames outside the two targeted changes. `grep badge-outline` on the file returns zero after this pass (was 2 before).
- 2026-04-10 — 10. sell-booth-active fourth pass — direct user feedback on the header zone of the live screenshot: the green "● LIVE" pill in the header and the centered "MARKET IS LIVE · 127 BUYERS SHOPPING" text below the stats block both communicate the same thing (market is live), and the user noted that elsewhere we already moved away from a green LIVE pill toward a "X shoppers" pill that carries both signals (status + count). User gave discretion: "either way, lets clean this mess up." Picked option (b) — apply the X-shoppers pattern here so this screen aligns with the elsewhere convention. Three edits: (1) header market-context pill replaced — `<div class="badge badge-success gap-1"><span class="inline-block w-1.5 h-1.5 rounded-full bg-success-content"></span>LIVE</div>` (3-line nested badge with the loud green fill, the success-content dot, and the literal "LIVE" word) collapsed to `<div class="badge badge-outline">127 SHOPPERS</div>` (single-line outline badge carrying the shopper count, the count itself implies liveness — you don't have shoppers if the market isn't live); (2) the centered subline `<div class="text-[10px] uppercase tracking-widest text-base-content/60 mt-2 text-center">Market is live · 127 buyers shopping</div>` deleted entirely from the stats section — the new pill carries both the "live" signal and the shopper count, so this line is now fully redundant; (3) action-bar button label `+ Add` changed to `+ Add Listing` for clarity (the button class stays `btn btn-sm btn-neutral`, only the text changes). Note on T13: the previous T13 proposal was a lightning-bolt icon-circle for live + calendar circle for upcoming. The user is now describing an evolved pattern: a count-bearing pill ("X shoppers") that uses the count itself as the liveness signal, no separate icon needed. This pass applies the evolved pattern to this screen only — the cross-screen sweep that touches buy-feed, item-detail-buyer, item-detail-buyer-inquiry, item-detail-dealer-browsing, sell-market-picker, and sell-add-item is still pending and stays deferred. T13 entry in PHASE_1_REVIEW_NOTES.md should be updated in a future cross-screen pass to reflect the X-shoppers convention. CLAUDE.md "no new HTML elements" rule respected — the badge element existed, only its inner structure and label changed; the subline was a deletion not an addition; the button label change is a text edit. No inline styles. `grep badge-success` on the file returns zero after this pass (was 1 before — the old LIVE pill).
- 2026-04-10 — 10. sell-booth-active fifth pass — user spotted a logic flaw in the per-row action buttons: "how are you deciding which items get to have a 'mark as sold' button?" The original Phase 1 wireframe spec only put `Mark Sold` on items that had been price-dropped (Item 5, Pair of Cane Chairs) or were HELD (Item 2, Teak Nesting Tables). Items at full live price (Item 1 Walnut Credenza, Item 4 Oak Rolltop Desk) got `[Hold] [Drop Price]` instead. That rule is wrong: walk-up full-price sales are the most common transaction in a live antique market, and the most important dealer action ("a buyer just handed me cash for this") should be available on every non-terminal item, not gated on whether the item has been price-dropped. Three options were considered: (A) drop `Drop Price` from the row and move it into the item detail screen; (B) drop `Hold` from the row and rely on inquiry-driven holds from the inquiry detail screen; (C) cram three buttons into every row. Picked (A) — Mark Sold and Hold are both reactive-to-real-world actions ("a buyer just walked up", "a buyer just DMed me") and belong on the quick-action surface, while Drop Price is a deliberate thinking-time action that's fine living one tap away in the item detail screen. New rule: every non-terminal item shows exactly two quick-action buttons, `[Hold] [Mark Sold]` (or `[HELD] [Mark Sold]` when the row is in held state). SOLD rows stay terminal with the single disabled `[SOLD]` button. Two edits: Item 1 (Walnut Credenza) `Drop Price` → `Mark Sold`; Item 4 (Oak Rolltop Desk) `Drop Price` → `Mark Sold`. Item 5 (Pair of Cane Chairs) already had `[Hold] [Mark Sold]` from the original spec — unchanged. Items 2 and 3 unchanged. Same two edits applied to the parallel `_scratch/sell-booth-active-cards.html` exploration so the card grid stays in sync with the row-action rule. The price-drop strikethrough on Item 5 ($240 → $180) is preserved — it's a state indicator showing this item was already dropped, not an action affordance, so removing the row-level Drop Price button doesn't conflict with showing past drop history. New product implication for the item detail screen: `item-detail-dealer-own` needs to be the canonical place to drop a price (it already is, this just makes that the only place). Future cross-screen pass should verify the dealer-own detail screen exposes price-drop clearly. `grep "Drop Price"` on `public/wireframes/` after this pass returns zero. No inline styles. No class renames.
- 2026-04-10 — 10. sell-booth-active sixth pass — user picked the card grid layout from the side-by-side compare exploration: "lets go ahead with the card version on the right." Promoted `_scratch/sell-booth-active-cards.html` to be the canonical `public/wireframes/sell-booth-active.html`, replacing the list view entirely. Path fix during promotion: `EARLY BIRD` logo link rewritten from `../wireframes/home-dealer.html` (scratch-relative) to `home-dealer.html` (canonical wireframes-folder relative). Page `<title>` changed from "Early Bird — Active Booth (Card Grid)" back to "Early Bird — Active Booth" to match the other wireframes. The card grid carries forward all the rules established in passes 1-5: outline state indicators merged into filled buttons in the card body (HELD, SOLD); no filter row; no per-item LIVE pill; no Edit button; watcher/inquiry counts inline in a meta line; `127 SHOPPERS` outline pill in the header; `+ Add Listing` action bar; `[Hold] [Mark Sold]` quick actions on every non-terminal card (Drop Price moved to item detail per pass 5). Card structure mimicked from `buy-feed`: `card card-compact bg-base-100 border border-base-300` shells in a `grid grid-cols-2 gap-3`, `aspect-square bg-base-200` photo placeholders, two action buttons stacked vertically (`flex flex-col gap-1`) in the card body. Adapted for dealer context: no dealer-avatar overlay (it's the dealer's own booth), no heart button (dealer doesn't favorite their own items), meta line carries "X watchers · Y inquiries" instead of the dealer name. SOLD card uses `opacity-60` on the whole card plus line-through on title/price plus a disabled `[SOLD]` button. FAB + bottom nav preserved unchanged. Cleanup: deleted both scratch artifacts (`_scratch/sell-booth-active-cards.html` and `_scratch/sell-booth-active-compare.html`) since the comparison is resolved and the canonical artifact has moved to `wireframes/`. The `buy-feed-compare`/`buy-feed-proposed` scratch pair stays in place — that comparison was never resolved, no canonical promotion happened. New cross-cutting gap surfaced this pass: empty states. The card grid and the list view it replaced both assumed a populated booth — there is no "you haven't listed anything yet" state. Logged as a new follow-up checklist item for a dedicated cross-screen pass that covers all screens with lists/grids. No inline styles. No class renames from the scratch source.
- 2026-04-10 — 11. sell-add-item sixth pass (header Row 1 via /design-review) — direct user feedback with two screenshots of the current header after pass 5: "its so strange to me that you're using button styling like this cancel button for mobile. It just looks misaligned until you hover and see the bg. but on mobile you don't really see the bg until its pressed. In general the top section of this pages feels off and poorly designed. It feels small. no hierarchy. and it feels different from the other headers you've build. /design-review can you look at this page and give notes within the context of what we're doing elsewhere on this site." Ran /design-review skill (focused, skipped the full bootstrap for this wireframe-only context). Grepped the wireframes folder for all `<header>` blocks and read the canonical tab-header pattern on sell-booth-active, buy-feed, watching, account-buyer, account-dealer. The canonical Row 1 pattern is a single text element at `text-lg font-bold tracking-tight` (always the biggest thing in the header, acts as the hierarchy anchor) paired with a ~32px circle on the right (avatar on tab screens, could be a close button on modal-form screens). sell-add-item's Row 1 was doing none of this: the "Add Item" title was at `text-sm font-bold uppercase tracking-wider` (same visual weight as the Row 2 "Downtown Modernism" market name and the countdown digits, so Row 1 had no hierarchy anchor whatsoever — the entire header read as four text-sm siblings with nothing to anchor the eye), the `btn-ghost` Cancel button rendered as floating text on mobile because btn-ghost's bg-on-hover treatment has no effect where there is no hover state, and the `w-14` right-side spacer was a magic number that didn't even correctly balance the Cancel button's actual width (so the title was visually off-center). Three issues, one root cause: Row 1 was built without reference to the canonical header pattern. User reviewed the diagnosis notes and picked Option A from the /design-review fix options (AskUserQuestion flow): text-lg uppercase title + circle ✕ button. Fix: `<button class="btn btn-sm btn-ghost text-xs uppercase tracking-wider">Cancel</button>` → `<div class="text-lg font-bold tracking-tight">ADD ITEM</div>` (promoted from text-sm to text-lg, dropped the btn/button element for the title since it wasn't actually interactive, changed content from "Add Item" mixed-case to "ADD ITEM" uppercase content so it visually matches "EARLY BIRD" letter-for-letter in mono at the same class); `<div class="text-sm font-bold uppercase tracking-wider">Add Item</div>` (the old title div) deleted entirely; `<div class="w-14"></div>` spacer deleted (no longer needed, the real 32px close button on the right naturally balances the row via flex justify-between); new `<button class="btn btn-sm btn-circle btn-ghost bg-base-200 border border-base-300 text-base-content/60">✕</button>` added to the right side. The new close button uses `bg-base-200 border border-base-300` for explicit visible affordance at rest on mobile (fixes the "invisible until tap" bug), btn-sm for 32px size matching sell-booth-active's avatar circle, btn-ghost for the hover/active state primitives without the ghost's transparent default. sell-add-item and sell-booth-active now share identical Row 1 structural weight distribution: one text-lg text element + one 32px circle on the right, with text-sm market context on Row 2. Third cross-reference read this session (after sell-booth-active in pass 3 and landing-buyer in pass 4) — logged here for the reviewer. The user's pass-3 directive "make the two pages look like they're related" is now more fully satisfied than it was after pass 3, because pass 3 only matched the 2-row structure but kept Row 1's weak content treatment — this pass fixes the content treatment to actually match. Semantic change from "Cancel" word to ✕ icon: Cancel implies form-abandon (discard changes), ✕ implies modal-dismiss, functionally equivalent for this screen (the bottom sticky CTA is Post Item, so tapping the X can only mean "abandon this form"); the word "Cancel" wasn't doing enough work to justify keeping a broken button treatment. CLAUDE.md "no new HTML elements" rule: the 3-element Row 1 flex container (button + title + spacer) became a 2-element Row 1 (title + button) — this is element restructuring within the existing flex wrapper, the title's `<div>` was already a div (now content and classes changed), the spacer `<div>` was deleted, and the Cancel `<button>` became an X `<button>`. Net element count decreased from 3 to 2. No new wrappers introduced. No inline styles. `grep 'style="'` returns zero on the file after this pass.
- 2026-04-10 — 11. sell-add-item fifth pass (design review) — user: "think like a designer and review this page and mage it look as good as possible? im talking about hierarchy, structure, spacing, rhythm, copy, and overall design." Audit found three issues below the photos section: (a) vertical rhythm broken — Title section used `px-4 py-4`, Description and Price used only `px-4 pb-4` (no pt), creating asymmetric spacing between three sibling form fields; (b) the Price Firm checkbox sat in its own `form-control mt-3` block below the price input, visually orphaned from the field it modifies; (c) the Price label lacked the required `*` marker that Title already carried, inconsistent required-field signaling. Fix (one coherent structural change): consolidated the three separate form `<section>` wrappers (Title, Description, Price) into a single `<section class="px-4 py-5 space-y-5">` so the form section mirrors the photos section's `py-5` — even 20px rhythm above the first field, between the three fields, and below the last field. Moved the Firm toggle inline into the Price label's `label-text-alt` slot: `<label class="label-text-alt cursor-pointer flex items-center gap-2">` wrapping a `checkbox-xs` input and `<span class="text-[10px] uppercase tracking-widest font-bold">Firm</span>` — the mode toggle now lives on the field it modifies, at the same typographic weight as the char-counter slot on Title/Description. Added `*` to the Price label so both required fields (Title, Price) carry the same required marker. Net element change: 3 wrapper elements deleted (2 `<section>` wrappers for Description and Price that got absorbed into the new Details section + 1 `<div class="form-control mt-3">` wrapper for the old Firm block). No new content, no new classes beyond what DaisyUI/Tailwind already expose, no inline styles, no header touches, no photos touches, no sticky-bottom touches. Sticky `h-36` spacer unchanged — new form is slightly shorter than the old one (removed Firm block saves ~24px) so the spacer still clears the fixed bottom comfortably. `grep 'style="'` returns zero on the file after this pass. User's "look as good as possible" directive read as a scope expansion authorization for restructuring the form's section wrappers (which would normally be protected by the CLAUDE.md "no new HTML elements" rule) — the restructuring is a consolidation of existing elements into a simpler structure, not an addition, so the rule's spirit (don't invent new functionality) is respected.
- 2026-04-10 — 11. sell-add-item fourth pass — direct user feedback after the header-unification pass: "I like it. but the 'drops fri 6pm' thing should look like the countdown that we use elsewhere." Grepped the wireframes folder for existing countdown treatment and found the canonical pattern on `landing-buyer.html` lines 77-78 (repeated in the 3 upcoming-market cards): a `text-xs uppercase text-base-content/60` label reading "Drops in" paired with a `font-bold text-sm tabular-nums` time display formatted as `01D 14H 32M 09S`. The `tabular-nums` class keeps the digit widths uniform so a ticking countdown doesn't jitter. Replaced the `<div class="badge badge-outline">DROPS FRI · 6PM</div>` pill on sell-add-item's market-context row with the label+time combo wrapped in a `<div class="flex items-center gap-2">` so they sit inline on the right side of the row: `<div class="text-xs uppercase text-base-content/60">Drops in</div>` + `<div class="font-bold text-sm tabular-nums">01D 14H 32M 09S</div>`. Reused the exact countdown value from landing-buyer's first market card to keep the two screens' numbers consistent (the two screens are likely showing the same market at different moments — the dealer setting up vs the buyer browsing the upcoming drop). Row 2 still reads `Downtown Modernism | Drops in 01D 14H 32M 09S` as a flex-between, preserving the 2-row header structure established in the third pass. The live-state analog (127 SHOPPERS outline pill on sell-booth-active) and the pre-drop state (this countdown) now occupy the same slot on parallel screens, each using the right visual primitive for its state: pill for live count, countdown for time-to-drop. Classes copied verbatim from landing-buyer to keep the countdown primitive single-sourced. CLAUDE.md "no new HTML elements" rule: the market-context row already existed as a flex-between; the right-side slot was a `<div class="badge badge-outline">`, now it's a `<div class="flex items-center gap-2">` wrapping two `<div>`s. This is a content/class change inside the existing slot, not a structural addition to the screen. Second cross-reference read this session (after sell-booth-active in the third pass) — logged here for the reviewer. `grep 'style="'` returns zero. `grep 'badge-outline.*DROPS'` returns zero.
- 2026-04-10 — 11. sell-add-item third pass (header unification with sell-booth-active) — direct user feedback after the follow-up pass: "this looks terrible. Can you please review the header on the dealers inventory page and make the two pages look like they're related. Think like a designer." Explicit user directive to cross-reference `sell-booth-active.html` — this is a deliberate exception to the one-screen-per-session rule, logged here so the next reviewer can see the context. Read `sell-booth-active.html` for reference only; no edits to that file. sell-booth-active's header is a single `<header class="px-4 pt-6 pb-3 border-b border-base-300">` containing two rows with `mb-3` between them: row 1 is `EARLY BIRD | avatar`, row 2 is `Downtown Modernism (text-sm font-bold) | 127 SHOPPERS (badge badge-outline)`. Key properties: (i) no eyebrow/label above the market name, (ii) market name is just the market name — no trailing date suffix, the pill carries the temporal information, (iii) no separate `bg-base-200` market-context panel, the market context lives INSIDE the header, (iv) consistent padding and a single bottom border under the whole header block. Applied the same structural pattern to sell-add-item: merged the separate `<section class="px-4 py-3 bg-base-200 border-b border-base-300">` market-context panel INTO the `<header>` as a second row. The header is now `<header class="px-4 pt-6 pb-3 border-b border-base-300">` with row 1 `Cancel | ADD ITEM title | spacer` (the form-screen chrome) and row 2 `Downtown Modernism | DROPS FRI · 6PM` (the market context). Dropped: (a) the `bg-base-200` panel treatment on the market context (now lives inside the header with the same white background as everything else), (b) the `ADDING TO` eyebrow label (the screen chrome "ADD ITEM" + the form itself make the "adding to this market" context self-evident), (c) the `· Sat Apr 26` date suffix on the market name (the pill carries the temporal info — "DROPS FRI · 6PM" — so the name row doesn't need to repeat it; this also matches sell-booth-active which shows just "Downtown Modernism"). The row 1 structure differs from sell-booth-active (Cancel+title+spacer vs logo+avatar) because sell-add-item is a form screen with a Cancel exit, not a tab-screen with nav chrome — the 2-row header PATTERN matches even though row 1's content is role-appropriate to each screen. Typography alignment: market name is `text-sm font-bold` on both screens; badge is `badge badge-outline` with identical treatment on both screens. This pass is specifically a design cohesion pass — the user's earlier content-level feedback (drop photos header, pre-drop state, X/Add weights) landed in the follow-up pass; this pass is about making the dealer's add-item form and the dealer's inventory view feel like the same product. CLAUDE.md "no new HTML elements" rule: the merge is a restructuring of existing elements (the market-context div moved from a sibling `<section>` into the `<header>`) — nothing added, just reparented. `grep 'style="'` returns zero.
- 2026-04-10 — 11. sell-add-item follow-up pass — direct user feedback on the first pass: (1) "PHOTOS *" section header is pointless / obvious, (2) the ✕ buttons on photos are the boldest thing on the page (should be Add instead), (3) "First photo = main" is redundant, (4) the wireframe is currently showing post-drop state ("Live instantly") but would be more useful showing pre-drop state. Four restructurings: (a) **Photos section header deleted entirely** — the entire `<div class="flex items-center justify-between mb-3">` wrapper containing "PHOTOS *" and "First photo = main · Drag to reorder" removed. The photo grid is now the first thing in the section with no preceding label, which the user's point 1 explicitly endorses. Side effect: the photo-required `*` marker is gone. The only visible required marker left in the form is `*` on Title; photos stay required implicitly (Post button disabled without one is Phase 3 wiring) and the photo grid's purpose is self-evident as the top of the form. (b) **X ↔ Add visual weight swap** — X buttons on all 4 photo placeholders switched from `btn btn-circle btn-xs btn-neutral` (dark fill, boldest thing on the page) to `btn btn-circle btn-xs btn-ghost bg-base-100 border border-base-300 text-base-content/60` (subtle white circle with light border and muted character); Add button switched from `border-2 border-dashed border-base-300 ... text-base-content/60` to `border-2 border-dashed border-neutral ... text-base-content` (dashed border color darkened from light gray to neutral, full-strength text replacing muted text); `+` character and "Add" label both gained `font-bold`. Kept the dashed border on purpose: dashed border is the universal "placeholder / empty slot / add-to" visual convention, and the solid `border-2 border-neutral` treatment is already owned by the main thumbnail slot (Photo 1). A first take on this pass used solid `border-2 border-neutral` on Add to make it maximally bold, but that collided visually with the main thumbnail's border and the user flagged it — fix is to keep the dashing (preserves placeholder semantics, differentiates from filled thumbs) while darkening the color and bolding the text (delivers the "Add is louder than the ✕" outcome the original feedback asked for). Hierarchy inverted: the primary action (Add) is now visually louder than the destructive action (✕ on an existing photo), which matches intent — you add photos way more often than you remove them. (c) **Market state flipped pre-drop** — `<div class="badge badge-success badge-sm gap-1">` with the success-content dot and literal "LIVE" text replaced with `<div class="badge badge-outline">DROPS FRI · 6PM</div>` (outline text pill showing the scheduled drop time: the market is Sat Apr 26, drops happen the evening before per EB_DESIGN.md "The drop" section, so Fri 6PM is the drop moment). This is NOT the T13 cross-screen LIVE-pill replacement — T13 is replacing the LIVE indicator style across all screens and stays deferred. This pass is changing the WIREFRAME STATE (the state this screen shows) from post-drop to pre-drop; the pill content legitimately changes because the state changed. The outline treatment matches the X-SHOPPERS evolution on sell-booth-active pass 4 and stays T2-compliant. (d) **CTA helper text flipped pre-drop** — "Live instantly · Watchers get notified" → "Goes live when the market drops · Fri 6PM", matching the new pre-drop wireframe state. The OQ1 HTML comment above the helper text inverted its polarity: was "Post-drop (current wireframe state, market is LIVE)" + "Pre-drop: ..." is now "Pre-drop (current wireframe state, market has not yet dropped)" + "Post-drop (market LIVE): ...". Phase 3 still needs to swap the copy based on market state; the comment documents both variants. Rationale for flipping the canonical state: the user's point (4) is that the add-item experience is more commonly entered BEFORE the drop than after — dealers load their inventory in the days before the market, not during. The pre-drop state is the one dealers will see 90% of the time, so the wireframe should show that. First-pass revision (c82b5dc) stays in the log — this is an additional pass on the same screen driven by direct user feedback on the first pass, not a redo. CLAUDE.md "no new HTML elements" rule respected: (a) is a deletion, (b) is class-name changes on existing elements, (c) is content change inside an existing badge element, (d) is content change inside an existing `<p>` and HTML comment. No inline styles introduced. `grep badge-success` on the file returns zero after this pass (was 1 before — the old LIVE pill). `grep 'style="'` on the file returns zero.
- 2026-04-10 — 11. sell-add-item — MAIN photo badge switched from `badge badge-neutral badge-sm` → `badge badge-outline` (T2/T3 — drops the filled fill and the tight `badge-sm` padding in one move; same pattern 05/10 converged on). Photos and Title labels gained required `*` markers per 11.4 — "PHOTOS *" in the section header, "TITLE *" in the form label alt slot; description/price/price-firm explicitly not marked, matching the "only picture and title required" rule (Category was removed in an earlier cross-session pass per the 2026-04-10 sell-add-item log entry — grep confirms zero `category` references in this file). The "· No haggling at booth" helper next to the Price Firm checkbox removed entirely (11.3) — the checkbox label now reads just "Price Firm". OQ1 annotated as an HTML comment above the sticky-CTA helper text, so Phase 3 wiring knows to swap "Live instantly · Watchers get notified" to "Goes live when the market drops · Sat Apr 26, 6:00 PM" when the target market is not yet LIVE (11.2 — note/annotation only, no wiring). T10: nav bg switched from `bg-base-100` → `bg-base-200` so the CTA+nav sticky block is a single unified panel, matching 09/10 and the rest of the revised screens. T1 (11.1) N/A by confirmation — the file already has the "Sticky bottom: CTA + nav (single container, T1+T10)" structure in place from an earlier pass; the outer `fixed bottom-0 left-0 right-0 bg-base-200` wrapper holds both the CTA block and the nav, so there's no gap for page content to scroll through; the T10 nav-bg switch above is what makes the unified panel actually look unified. T7 (11.5) N/A — `grep -i 'pre-buy|preview|pre-market'` on this file returns zero matches. T8 (11.6) N/A — there is no EARLY BIRD logo element in this screen's header (header is `Cancel · Add Item · spacer`), same call as 07/08/09, do not invent a logo to satisfy T8. T4 N/A — the "Cancel" top-left is a labeled text button (`btn btn-sm btn-ghost` with "Cancel" text), not a tiny icon button; the photo-remove ✕ buttons inside the photo grid are content-level controls, not page chrome, so T4 (which targets back/close in page chrome + sheet close X's) doesn't apply. T13 deferred — the "LIVE" market-context pill (`badge-success` with dot + "LIVE" text) is explicitly part of the cross-screen T13 pass and not touched here. `grep 'style="'` on the file returns zero.
- 2026-04-11 — 13. account-buyer — header Row 1 brought into line with the canonical tab-screen structure established in `sell-add-item` pass 6: the right slot was a `text-xs uppercase tracking-widest text-base-content/60` "Account" eyebrow div, now replaced with a 32px `avatar placeholder` containing the user's "JC" initials in a `bg-neutral text-neutral-content w-8 rounded-full` circle (matches `sell-booth-active`'s header avatar slot exactly — `w-8` = 32px, same `bg-neutral` fill, same `text-xs font-bold` initials). Left slot promoted from a static `<div>` to an `<a href="home-buyer.html">` link wrapping the unchanged `text-lg font-bold tracking-tight EARLY BIRD` content (T8 — buyer's logo routes to home-buyer lobby). The 32px header avatar is intentionally a duplicate of the larger w-16 in-page profile avatar below it; the duplication is acceptable because the header avatar is a structural nav element that exists on every tab screen for consistency, while the in-page avatar is the profile content. T9 (13.1, canonical source on this screen): profile-section `<button class="btn btn-sm btn-outline">Edit</button>` deleted entirely; the `font-bold text-lg` John Chen `<div>` replaced with `<input type="text" value="John Chen" class="input input-ghost font-bold text-lg w-full px-0 h-auto min-h-0 py-0 focus:outline-none" />` — the DaisyUI `input-ghost` primitive renders without background until focused, the `px-0 h-auto min-h-0 py-0 focus:outline-none` overrides strip the input's default form-control padding and height so it visually matches plain text at rest, and the `font-bold text-lg` preserves the original typographic weight. Phone stays as a readonly `<div>` per T9's "phone (readonly/verified)" guidance — this screen doesn't have a verified-status indicator next to the phone yet, but adding one would be a content-level addition that the per-screen note didn't ask for, so deferred. The `Markets You Follow` `<button class="btn btn-xs btn-ghost">Edit</button>` also deleted (T9 applies to every section that previously had an Edit affordance). The market-section header collapsed from `<div class="flex items-center justify-between mb-3">` two-child layout to a single label `<div>` since there's no longer a second element to justify. T2/T3 (13.4): three market pills converted from `<div class="badge badge-neutral gap-2">` to `<div class="badge badge-outline px-3 py-3">` — drops the filled neutral fill (T2 outline-down), drops the no-op `gap-2` (single-child badge has no internal gap to control), adds explicit `px-3 py-3` padding so monospace market names breathe (T3). Casing on the existing pills was already consistent Title Case ("Downtown Modernism", "Rose Bowl Flea", "PCC Flea") — no camelCase to fix. The "+ Follow More" affordance (13.2): renamed to "+ Add Market" so the action is unambiguous (the old "Follow More" copy could read as "follow additional things in the abstract"; "Add Market" is clearly "add another market to the list"). Element promoted from a `<div class="badge badge-outline gap-2">` to a `<button class="badge badge-outline border-dashed px-3 py-3">` — buttonized to make the interactive affordance explicit, dashed border to differentiate the add slot from the populated pills (universal placeholder/add convention), same `px-3 py-3` padding for visual rhythm with the pills it joins. T8: EARLY BIRD logo routed to `home-buyer.html` per the canonical Row 1 fix above (this screen's instance of T8 was the same change as the header restructure). T10: bottom nav switched `bg-base-100 border-t border-base-300` → `bg-base-200`, matching the convention 04/05/06/07/08/09/10/11 all converged on. The vertical `<div class="w-px bg-base-300 my-2"></div>` divider between Watching and Account in the nav is preserved — it provides visual separation between the two halves of the buyer nav (Buy/Watching | Account), and `bg-base-300` still reads against the new `bg-base-200` background. 13.3 (Become A Dealer copy clarity): the per-screen note says "Tie it to the dealer-apply flow and rewrite the copy to match" but explicitly references OQ3, which is in the Open Questions section governed by the protocol rule "Do not try to answer them inside a revision session — flag them in the commit message, leave the current behavior alone". Resolved this tension the same way pass 2 of sell-add-item resolved OQ1: added an HTML comment annotation above the Upgrade `<section>` flagging that the copy + button label are blocked on OQ3, left the current "Selling at a market? / Add a dealer account to post inventory before the drop. / [Become A Dealer]" copy intact pending resolution. The next session that resolves OQ3 will rewrite the copy to match whatever flow is decided. T1 N/A — there is no sticky CTA stacked above the bottom nav on this screen; the nav is the only fixed-bottom element, no gap glitch to fix. T4 N/A — no back/close button in page chrome (this is a top-level Account tab, not a sub-screen). T5 N/A — no debug/state labels in the header (the old "Account" eyebrow was a legitimate page label, not a `State N` dev string, but it's gone now anyway because the canonical Row 1 replaced it with the avatar circle). T6 N/A — no filter row. T7 N/A — `grep -i 'pre-buy|preview|pre-market'` on this file returns zero matches. T11 N/A — no payment handles on the buyer account screen (T11 applies to dealer-side payment-method UI only). T12 N/A — no modal/drawer on this screen (account screens edit in place per T9, which this session implements). T13 N/A — no LIVE pill on this screen. CLAUDE.md "no new HTML elements" rule: every change is either a deletion (Edit buttons), a transformation of an existing element's tag/classes/content (div → a, div → input, div → button, badge-neutral → badge-outline), or an HTML comment annotation. No new wrappers, no new sections, no inserted markup beyond the comment. `grep 'style="'` on the file returns zero. Visual verification: file edits applied to `public/wireframes/account-buyer.html`, served HTML confirmed via `curl http://localhost:3001/wireframes/account-buyer.html` matches the on-disk file. preview_* MCP tools not used because the user's existing Next.js dev server is already on port 3001 (not preview-managed) and starting a parallel preview-managed server would have either required killing the user's session or running two competing dev servers; opted for direct curl verification of the static HTML instead since this wireframe has no JS state to render.
- 2026-04-11 — 13. account-buyer second pass: design review — direct user feedback after the first pass landed: "vertical rhythm, hierarchy, overall design. I don't like the way notifications section looks. I feel like there's lots of different styles of buttons and strokes and stuff across the page. It just looks clumsy". /design-review invoked on the live wireframe via preview_start (eb-v2 launch config updated to use `/bin/zsh -c` wrapper with explicit `/opt/homebrew/bin` PATH so the spawned npm process can find node — the bare `npm` runtimeExecutable was failing with `env: node: No such file or directory`; previous orphaned dev server on port 3001 from the first-pass session was killed before starting the preview). Four targeted fixes: (a) **Notifications breathing** — the three `<div class="form-control">` toggle rows were rendering 36px tall touching each other (DaisyUI `.label` has `padding: 0.5rem 0.25rem` default but the form-controls had no margin between siblings), so the toggles + descriptions felt jammed into a single block. Wrapped all three form-controls in a `<div class="flex flex-col gap-4">` parent for explicit 16px gap between rows; bumped the toggle size from `toggle toggle-sm` to default `toggle` so each row has more visual weight to anchor against; set the inner `<label>` padding to `p-0` so the explicit gap fully owns the row spacing (no double-padding fight); bumped the section label margin from `mb-3` to `mb-4` for symmetric breathing above the toggle stack. Same `mb-4` applied to the Markets section label for consistency. (b) **Killed all between-section dividers** — there were four `<div class="divider mx-4 my-0"></div>` lines between Stats/Notifications, Notifications/Markets, Markets/Upgrade, and Upgrade/Footer. Each divider was a horizontal hairline fighting the section padding for visual weight, creating the "clumsy" gestalt (two competing separation primitives doing the same job at the same time). Removed all four. The footer divider on line 143 (`<div class="divider my-2">` between the footer links and the Sign Out button) is preserved because it lives INSIDE the footer section that's already flagged for redesign in the first-pass TODO comment — touching it would conflict with the deferral. (c) **Normalized vertical rhythm** — section padding was a mix of `pb-4`, `py-5`, `py-5 pb-8`. Standardized to `py-6` on Notifications/Markets/Upgrade so the post-identity sections breathe at a consistent 24px cadence. Profile section kept at `pt-6 pb-3` (slightly tighter to bind to the header) and Stats section bumped from `pb-4` to `pb-6` so the identity group (header avatar → in-page avatar+name+phone → stats) reads as a single tight unit with a clean bottom margin before the Notifications group begins. Result: the page now has two rhythm tiers — tight inside the identity group, consistent `py-6` breathing between the four post-identity groups (Notifications, Markets, Upgrade, Footer-deferred). (d) **Dropped the dead Upgrade-card border** — `<div class="card card-compact bg-base-200 border border-base-300">` had a `border border-base-300` class pair that wasn't rendering visibly against the `bg-base-200` fill (preview_inspect on `.card.bg-base-200` returned empty `border-color`/`border-width`/`border-style` styles, confirming the classes were dead code — likely DaisyUI `.card` overriding Tailwind's border utility). Removed `border border-base-300` entirely. The Upgrade card and the Stats box are now both clean `bg-base-200` fills with no outline, which gives the page exactly two intentional container treatments: gray-fill for informational containers (Stats, Upgrade) and outline for interactive tags (Market pills, Add Market). Two primitives with clear jobs replace five competing styles. Footer untouched — the existing TODO comment from the first pass still flags the entire footer block (`Help/Privacy/Terms` links + divider + Sign Out + version stack) for a follow-up redesign session; did not touch any element inside the footer `<section>`. CLAUDE.md "no new HTML elements" rule: change (a) is a wrapper added around three existing siblings (one new `<div class="flex flex-col gap-4">` parent — the only structural addition this pass); change (b) is four deletions; change (c) is class-name padding adjustments on existing `<section>` elements; change (d) is a class deletion on an existing `<div>`. The wrapper in (a) is the only new element and it has no visible chrome (just a flex container) — it doesn't add any visible content to the screen, just changes how the existing toggle rows are spaced. Justified by the user's explicit "fix this clumsy thing" feedback as the only viable way to insert a gap between three sibling form-controls without inline styles. T2/T3/T9/T10 already applied in pass 1, no re-application needed. `grep 'style="'` on the file returns zero. Visual verification via preview_screenshot before/after at mobile preset (375x812), preview_inspect confirmed the new `flex flex-col gap-4` container is `display:flex` and the John Chen `input-ghost` field still renders as plain text at rest (`background-color: oklch(1 0 0 / 0.05)`).
- 2026-04-11 — 13. account-buyer second pass REVERTED — direct user feedback after the second-pass commit (672e1d1) landed: "i noticed that you removed all the dividers. i think the page looks like things are floating now somewhat. The other issue is that now this page doesn't look like the other pages on the app". Confirmed via grep that the `divider mx-4 my-0` between-section pattern is the canonical project convention used by 8 other wireframes (account-dealer, item-detail-buyer, item-detail-buyer-inquiry, item-detail-dealer-browsing, item-detail-dealer-own, sell-add-item, sell-booth-setup, sell-market-picker). Read account-dealer's section structure as the closest sister screen and confirmed the canonical post-identity rhythm: Profile `pt-6 pb-4` → Business/Stats `pb-4` (no internal divider, identity group is one block) → divider → settings sections at `py-5` separated by dividers → footer `py-5 pb-8`. Also confirmed account-dealer's Notifications section uses the EXACT same structure I "fixed" on account-buyer: `text-xs ... mb-3` label, three `<div class="form-control">` siblings each containing `<label class="label cursor-pointer gap-3 justify-start">` with `<input type="checkbox" class="toggle toggle-sm" />`, no flex-gap wrapper, no `p-0` override. So the second-pass notifications "improvement" (flex-col gap-4 wrapper + bumped toggle size + p-0 label override) made account-buyer diverge from its direct sister screen — exactly the inconsistency the user flagged. Restored the entire file via `git checkout 830f9cc -- public/wireframes/account-buyer.html`, which rolls back ALL second-pass changes including the dead-border removal on the Upgrade card (the `bg-base-200 border border-base-300` pair on `<div class="card">` doesn't render visibly because DaisyUI `.card` overrides Tailwind borders, but keeping the markup pattern uniform with the rest of the file is more important than removing dead code in this context). The post-revert file is now byte-identical to the post-Phase-1 first-pass state at commit 830f9cc. Visual verification via preview_screenshot at mobile preset confirms the four between-section dividers are restored and the page rhythm matches the sister screen. The original user concerns from the second-pass session ("vertical rhythm, hierarchy, notifications clumsy, lots of different button/stroke styles") are NOT addressed by this revert — they remain open. The lesson for future passes: the project has a strong existing visual convention used across 8+ screens, and any "improvement" that diverges from that convention on a single screen will make that screen feel out of place rather than improved. A real fix for the "clumsy" feeling would need to either (a) be limited to changes that don't break cross-screen consistency, or (b) be applied as a coordinated pattern update across all screens that share the convention — which would require explicit authorization to break the CLAUDE.md "one job per session" rule and would belong in its own dedicated session. Flagged for follow-up discussion with the user before any further attempt. `grep 'style="'` on the file returns zero. `diff <(git show 830f9cc:public/wireframes/account-buyer.html) public/wireframes/account-buyer.html` returns zero (byte-perfect restore).
- 2026-04-11 — T11 HOTFIX. account-dealer payment methods rip-out — direct user catch during the cross-screen polish session (which was scrolling through account-dealer to verify the notifications polish): "where did this come from? ive explicitly said 10 times that we cannot have users' venmo or zelle accounts listed in the app. This section is purely informational. It's just indicating that the user does take this kind of payment. Nothing more. Theres no connecting , edit etc. Just check boxes". This is a violation of T11 / `feedback_eb_no_payment_middleman.md` / EB_DESIGN.md "Never the payment middleman" rule that the user has stated repeatedly. Root cause: `account-dealer.html` is one of the 5 unrevised dealer-side wireframes (it has never had its own Phase 1 revision pass — confirmed by grepping REVISION_LOG.md, which only references account-dealer as a sister screen for cross-reference reads, never as the subject of a pass). The violating markup is pre-T11 Phase 0 content that was authored before T11 was codified, and it slipped through because T11 was applied to revised dealer screens (sell-add-item, etc.) but never back-applied to account-dealer. Hotfix scope: payment-methods section ONLY on account-dealer.html, no other elements touched. Specific changes: (a) Venmo row: deleted the `<div class="text-xs text-base-content/60">@marcos-finds</div>` handle subtitle and the `<button class="btn btn-xs btn-ghost">Edit</button>`; (b) Zelle row: deleted the `<div class="text-xs text-base-content/60">marco@marcosfinds.com</div>` handle subtitle and the matching Edit button; (c) Apple Pay / Card row: deleted the `<div class="text-xs text-base-content/60">Not connected</div>` status subtitle and the `<button class="btn btn-xs btn-outline">Connect</button>` button; (d) collapsed all four rows from the previous `flex items-center justify-between` two-child structure (`flex items-center gap-3` content block + right-side button) to a single `flex items-center gap-3` row with just `[checkbox] [name]` — no right-side affordances at all because there are no right-side affordances allowed under T11; (e) bumped row padding from `py-2` to `py-3` for slightly more breathing room now that each row is shorter (one line instead of two-line stacked name+subtitle); (f) added a brief `text-[10px] text-base-content/60 mb-3 leading-relaxed` explainer below the section label reading "Buyers see which methods you accept. Payment happens in person at the booth." — this is the ONLY content addition this pass and it exists to make the methods-only intent explicit so a future Claude session doesn't re-interpret an "empty" payment section as needing handles or Connect buttons. Added an HTML comment above the section: `<!-- Payment methods (T11: methods-only, no handles, no Edit/Connect — app is never the payment middleman) -->` so the rule is enforced inline at the markup level for any future editor. Net element change: 6 elements deleted (3 handle subtitles + 3 right-side buttons), 1 element added (the explainer div), 4 row wrappers restructured from 2-child justify-between to 1-child gap-3. CLAUDE.md "no new HTML elements" rule: this pass is a content-deletion driven by a standing product rule that overrides the Phase 1 wireframe-preservation rule — T11 is codified in EB_DESIGN.md as a cross-cutting theme that takes precedence over the no-new-elements rule, and the explainer div is the minimum addition required to make the methods-only intent self-documenting. Notifications section on this file is UNTOUCHED in this commit — the cross-screen polish for notifications lands in the next commit, separately, so the T11 hotfix has clean isolated git history. `grep 'style="'` on the file returns zero. `grep '@marcos\|marcosfinds.com\|Not connected\|btn.*Connect\|btn.*Edit' public/wireframes/account-dealer.html` returns zero matches in the payment section after this pass. Visual verification via preview_screenshot at mobile preset confirms: section reads `PAYMENT METHODS / Buyers see which methods you accept. Payment happens in person at the booth. / [✓] Cash / [✓] Venmo / [✓] Zelle / [ ] Apple Pay / Card`. No handles. No Edit. No Connect. The user's standing rule is now reflected in the wireframe.
- 2026-04-11 — Cross-screen polish: notifications breathing on account-buyer + account-dealer (authorized to break "one job per session" rule). After the second-pass revert (commit 32dd855), the original "clumsy" feedback on the notifications stack was still open. Presented user with two paths: A) accept the current divider+packed-toggle convention as deliberate and live with it, or B) coordinated cross-screen polish that touches BOTH account screens together so the improvement lands as a pattern update rather than an outlier. User chose "Path b" with the explicit scoping I proposed: just the notifications layout + the dead Upgrade-card border on both account screens; NO divider changes (those are working — they're the project's section anchors); NO padding changes (they match across screens); the smallest possible cross-cut. Two changes applied identically to both `account-buyer.html` and `account-dealer.html`: (a) **Notifications stack breathing** — wrapped the three sibling `<div class="form-control">` toggle rows in a single `<div class="space-y-3">` parent to give an explicit 12px gap between rows (DaisyUI `.label` has `padding: 0.5rem 0.25rem` default and form-controls have no margin between siblings, so the rows were touching at zero gap with no air between). Bumped the toggle from `toggle toggle-sm` to default `toggle` so each row has more visual weight to anchor against (small toggles next to font-bold text + a description line read as visually under-weighted — the bigger toggle makes the row a single intentional unit). Added `p-0` to each row's `<label class="label cursor-pointer gap-3 justify-start">` (becoming `gap-4 justify-start p-0`) so the explicit 12px parent gap fully owns the row spacing without DaisyUI's default label padding fighting it. Bumped the `gap-3` between checkbox and content to `gap-4` for the same anchoring logic — bigger toggle, slightly bigger gap to its label. Bumped the section label margin from `mb-3` to `mb-4` for symmetric breathing above the new toggle stack. (b) **Dead Upgrade-card border drop** (account-buyer ONLY — account-dealer has no Upgrade card) — `<div class="card card-compact bg-base-200 border border-base-300">` had a `border border-base-300` class pair that wasn't rendering visibly because DaisyUI `.card` overrides Tailwind's border utility (preview_inspect on `.card.bg-base-200` returned empty `border-color`/`border-width`/`border-style`, confirming dead code). Removed `border border-base-300` entirely. The Upgrade card and the Stats box now both render as clean `bg-base-200` fills with no outline, giving the page two intentional container treatments: gray-fill for informational containers, outline for interactive tags. NOT touched by this pass (deliberate scoping): (i) all four `divider mx-4 my-0` between-section dividers on each screen — preserved exactly because they're the canonical project pattern used by 8+ wireframes and the previous attempt to remove them is what triggered the second-pass revert; (ii) all section padding (`pt-6 pb-4`, `pb-4`, `py-5`, `py-5 pb-8`) — left untouched because the current rhythm matches across both account screens and across the rest of the project; (iii) account-buyer's footer-redesign TODO area (Help/Privacy/Terms/Sign Out/version stack) — still flagged for a follow-up session; (iv) account-dealer's payment methods section — separate concern, addressed in the previous T11 hotfix commit (3abc6bd); (v) every other element on either screen (header, profile, business name, stats, booth defaults, market follows, footer) — out of scope for this targeted cross-cut. CLAUDE.md "one job per session" rule: explicitly broken with user authorization. The "one job" framing was creating a structural problem — single-screen polish kept producing outliers because the "right" treatment for the notifications stack only works if it lands consistently across both account screens at once. The user's "Path b" choice authorizes coordinated cross-screen pattern updates as a recognized exception to the rule when (a) the change is a pattern rather than a screen-specific fix, AND (b) the scope is tightly bounded to just the pattern in question, AND (c) the rationale is logged so it doesn't normalize as a pattern of its own. Net element change per file: 1 wrapper added (the `space-y-3` parent around the existing form-controls), 0 elements deleted, 4 elements with class adjustments (the section label, the three form-control labels). The single new wrapper has no visible chrome — just a flex container — and is the only viable way to insert a sibling gap between three form-control siblings without inline styles, so it's justified under the spirit of the "no new HTML elements" rule. `grep 'style="'` on both files returns zero. Visual verification via preview_screenshot at mobile preset (375x812) on both `account-buyer.html` and `account-dealer.html`: the four dividers are still in place on both, the notifications stacks now show three rows with bigger anchored toggles and 12px breathing between rows, the markup pattern is byte-identical between the two screens (cross-screen consistency restored), and the original user concerns from the second-pass session ("clumsy notifications, lots of different button/stroke styles") are addressed by the pattern landing on both screens rather than diverging.
- 2026-04-11 — 13.6 account-buyer Become A Seller section: card flattened to match page rhythm + copy rewrite per user direction. User caught the remaining "Selling at a market?" card on a mobile screenshot mid-preview and said it "looks bad" in context of the page: "The bigger issue is that in the context of the page is that it looks bad." Suggested new headline ("Wanna be a seller?") and new copy ("Are you selling at an upcoming market and wanna post here for free? Learn more here.") but explicitly flagged the suggestions as flexible ("These are just suggestions"). Root cause diagnosis: the existing `<div class="card card-compact bg-base-200">` treatment was the ONLY rounded/filled container on a page that's otherwise flat `<section>` blocks separated by `divider mx-4 my-0` hairlines — the card plus its block `btn btn-sm btn-outline mt-3` "Become A Dealer" button read as a marketing interstitial pasted into a settings page, competing with the page's section rhythm for visual weight and losing. The fix had to collapse the card into the flat-section pattern used by Notifications / Markets You Follow / Help & Support above and below it. Applied: (a) deleted the `<div class="card card-compact bg-base-200">` outer wrapper and its `<div class="card-body">` padding container — 2 wrapper elements removed; (b) restructured the content into a flat section matching the canonical page pattern: `<section class="px-4 py-5"> / <div class="text-xs uppercase tracking-widest text-base-content/60 mb-3">Become A Seller</div> / <div class="text-sm leading-relaxed">[conversational copy with inline link]</div> / </section>`; (c) replaced the `<button class="btn btn-sm btn-outline mt-3">Become A Dealer</button>` block button with an inline `<a href="#" class="link font-bold">Learn more →</a>` sitting at the end of the body sentence — the DaisyUI `.link` primitive gives always-underline treatment (vs `.link-hover` which underlines on hover only) and `font-bold` anchors the visual weight for the interactive affordance without needing a button chrome; (d) rewrote the copy to match the user's conversational voice suggestion: body reads "Are you selling at an upcoming market and wanna post here for free?" followed by the inline "Learn more →" link. Chose "Become A Seller" (uppercase, formal) for the section label rather than the user's suggested "Wanna be a seller?" headline because (i) uppercase section labels are the page's canonical section-identification pattern and questions in all-caps read as shouty, (ii) the user explicitly flagged the copy suggestions as flexible, (iii) the conversational voice is preserved in the body sentence where it reads naturally at sentence-case size, and (iv) using "Become A Seller" keeps the label structurally parallel to "NOTIFICATIONS" and "MARKETS YOU FOLLOW" above it. "Wanna" retained in the body copy because the user explicitly used it and it gives the section its conversational character — the section label can be formal while the body is casual. Updated the OQ3 HTML comment above the section to note that the copy came from user direction in the 13.6 pass, and that the exact dealer-apply flow (one-tap toggle vs. application + review) is still TBD — the new "Learn more" link treatment leans toward the application+review flow (there's a page with more info behind the link) but the current `href="#"` placeholder doesn't commit to anything, so OQ3 remains open. Net element change: 2 wrappers deleted (card, card-body), 1 element added (the uppercase section label div), 1 element transformed (button → inline anchor), 1 element restructured (description div → body paragraph with inline link). CLAUDE.md "no new HTML elements" rule: the net element count is slightly DOWN (2 deletions vs 1 addition), and the added label div is a structural element that matches the page's existing section-identification pattern (replacing the card's visual role with the page's flat-label visual role). The user's "looks bad" feedback and explicit copy direction constitute the authorization to restructure. `grep 'style="'` on the file returns zero. `grep 'card\|btn.*outline' public/wireframes/account-buyer.html` returns matches only on the unrelated parts of the file (not in the Become A Seller section). Visual verification via preview_screenshot at mobile preset: the section now sits between Markets You Follow and Help & Support as a flat section with no visible container, the label reads "BECOME A SELLER" at the same weight/size as "MARKETS YOU FOLLOW" above it, the body paragraph reads as a single comfortable sentence with the "Learn more →" link bold+underlined at the end, and the page rhythm from Notifications → Markets You Follow → Become A Seller → Help & Support reads as four consistent flat sections separated by the canonical `divider mx-4 my-0` hairlines. The card-as-interstitial problem is resolved.
- 2026-04-11 — 13.7 account-buyer Markets You Follow: follow-state pill treatment replaces outline-only + dead "+ Add Market" affordance. Direct user feedback on mobile screenshot: "For 'markets you follow' the UI doesn't make sense. Theres nowhere for the 'add market' button to go. And also i don't like the way a bunch of black stroked pills look. How about using the dimmed gray ones for markets youre not following and solid black for the ones you are. And for now its ok because it'll be like four markets total." Two problems flagged: (1) the dashed `badge badge-outline border-dashed` "+ Add Market" pill was a button to nowhere — no `discover-markets.html` or similar destination exists in Phase 1, so the affordance was visually promising an action that couldn't happen, and (2) the three outline-only pills for followed markets gave no sense of state — there was no visual distinction between "followed" and "unfollowed" because only followed markets were shown, making the "You Follow" label redundant with the pill content. User's proposed treatment: show the full bounded LA-market list (~4 total) as toggleable pills with a clear two-state visual: solid black (following) vs. dimmed gray (not following). Bounded list is the unlock — there aren't so many LA markets that showing all of them in a settings page is noisy, and having all of them visible means the user can toggle any of them in place without needing a separate discover/browse screen. Applied: (a) **deleted** the `<button class="badge badge-outline border-dashed px-3 py-3">+ Add Market</button>` pill entirely — dead affordance goes away, and since the new pattern shows all markets in place with tap-to-toggle, there's nothing for an "add market" button to do; (b) **converted** the three existing followed-market pills from `<div class="badge badge-outline px-3 py-3">` to `<button class="badge badge-neutral px-3 py-3">` — `<div>` → `<button>` because the pills are now interactive toggles (previously they were static status indicators pretending to be interactive via their button-like visual); `badge-outline` → `badge-neutral` for the solid dark fill the user specified, which maps to the DaisyUI neutral semantic color (dark bg, light text) and is the darkest fill primitive on the page, giving clear "on" state weight; dropped the explicit `border` class (none needed because badge-neutral is filled, not outlined); (c) **added** one unfollowed market pill `<button class="badge badge-ghost px-3 py-3">Melrose Trading Post</button>` — `badge-ghost` is the DaisyUI primitive for a low-contrast subtle-fill badge, which delivers the "dimmed gray" treatment the user described and is visually distinct from `badge-neutral` without requiring a custom color stack; Melrose Trading Post is a real LA weekly flea (Fairfax High School lot, established market) chosen to give the wireframe a plausible unfollowed entry that a Pasadena/DTLA user might not follow; (d) added an HTML comment above the section documenting the new pattern — "Pills are follow toggles. Solid neutral = following, ghost/dimmed = not following." + "Full list of LA markets shown (bounded ~4 total); user taps a pill to toggle." — so a future editor understands the visual semantics and doesn't reintroduce "Add Market" or revert the two-state treatment. Visual contrast check via preview_screenshot: the three solid-neutral pills (Downtown Modernism, Rose Bowl Flea, PCC Flea) read as dark filled pills with white text, clearly visually distinct from the single ghost pill (Melrose Trading Post) which reads as a pale gray fill with muted-gray text — the "on/off" visual distinction is immediately legible without any additional labeling. Net element change: 1 element deleted (+Add Market button), 3 elements transformed (div→button + class change), 1 element added (Melrose Trading Post ghost button). CLAUDE.md "no new HTML elements" rule: the net element count is even (1 deleted, 1 added) and the 3 transformed elements are same-semantic-slot transformations (already-interactive-intent pills becoming actually-interactive buttons). The added Melrose pill is content addition (a new market name in the list) rather than new functionality — the follow-state slot pattern was always the point of the section, it was just previously showing only one state. The user's explicit direction ("dimmed gray ones for markets youre not following and solid black for the ones you are") constitutes authorization to add the contrasting-state example. NOT touched by this pass: (i) section wrapper classes (`px-4 py-5`), section label markup, dividers above and below — all preserved as canonical page rhythm; (ii) the `flex flex-wrap gap-2` pill container — unchanged, the `gap-2` rhythm between pills still works with the new two-state treatment; (iii) any other section on the page — tightly scoped to the pills. The name/phone edit discoverability issue raised in the same user message (T9 input-ghost pattern has zero visible affordance, user explicitly asked "If i was a user and i needed to edit my name or my number how would i do it?") is NOT addressed in this commit — it's a separate concern requiring a directional decision from the user on Option A (pencil icons) vs Option B (dotted underline + Change phone link) vs Option C (revert to a single Edit button), which was presented to the user in the same response. Waiting for user selection before editing the profile section. `grep 'style="'` on the file returns zero. `grep 'badge-outline\|+ Add Market' public/wireframes/account-buyer.html` returns zero matches in the Markets section after this pass (no lingering outline pills, no Add Market residue).
- 2026-04-11 — 13.8 Cross-screen notifications restructure (account-buyer + account-dealer, authorized cross-cut per established pattern). Direct user feedback on mobile screenshot of the buyer notifications section: "this section still bothers me. no structure. And the typography feels ugly." Two concrete diagnoses: (1) the 13.5 polish had bumped spacing between rows but left the toggle-LEFT + text-RIGHT-indented checkbox-form pattern intact, which is a form pattern rather than a settings pattern — real iOS/Android settings screens use label-LEFT + control-RIGHT (justify-between) which reads as "setting: state" and gives the list implicit structure via the right-aligned control column — the previous layout had no visual anchor organizing the rows beyond the vertical spacing; (2) the title `label-text font-bold` applied DaisyUI's 16px default + bold which in JetBrains Mono is EXTRA chunky because monospace bold adds significant visual weight — paired against the 12px muted description this created a typographic imbalance where the title read like a headline and the description read like an afterthought. Applied cross-screen to both `account-buyer.html` and `account-dealer.html` (same pattern reasoning as 13.5 — single-screen polish would produce an outlier; coordinated pattern update maintains cross-screen consistency). Five changes per screen: (a) **toggle moved to right** — label class `justify-start` → `justify-between`, which flips the internal layout so the title+description block sits on the left and the `<input type="checkbox" class="toggle" />` sits on the right, matching the iOS/Android settings convention and giving the rows implicit structure through right-column alignment; (b) **title typography** — removed DaisyUI `label-text` class which was forcing 16px default, replaced with direct `text-sm font-bold` (14px bold), which is still clearly a heading but less chunky in monospace bold weight; (c) **description typography** — removed `label-text` wrapper class, switched to direct `text-xs text-base-content/60 leading-tight mt-0.5`, which gives 12px muted text with tight line-height and a tiny 2px top margin so the description sits right under the title as a continuation rather than a floating second paragraph; (d) **row padding** — added `py-3` to each label so each row has 12px top+bottom internal padding, making each row a discrete breathing container instead of a flat text stack; (e) **between-row hairlines** — added `border-b border-base-300` to the first two rows (not the last, matching the payment-methods list pattern from the T11 hotfix) so the rows read as a structured list rather than three loose items in a vertical stack — the `base-300` color is very subtle (oklch(0.924...) = light gray) so the hairlines give structure without feeling heavy. Dropped the `space-y-3` wrapper entirely — the `py-3` + hairlines handle row separation now, and leaving `space-y-3` on top would stack with the padding and create uneven gaps. Dropped the `p-0` label override as `py-3` now fully determines the row padding. Kept: the `form-control` wrapper on each row (DaisyUI expects this for checkbox/label coordination), the `<label class="label cursor-pointer">` base classes (DaisyUI's label primitive), the section label `text-xs uppercase tracking-widest` classes (unchanged, but bumped margin from `mb-4` back to `mb-3` because the new `py-3` on the first row adds its own top breathing). Verification via preview_inspect on a row's computed style: `borderBottomColor: oklch(0.924169 0.00108 197.138)` (base-300), `borderBottomStyle: solid`, `borderBottomWidth: 1px`, `padding: 12px 0px` — all confirmed rendering as intended. Visual verification via preview_screenshot on both files: buyer notifications (Drop Alerts / Price Drops / New Markets) and dealer notifications (New Inquiries / Drop Reminders / Watcher Milestones) both render with the new label-left/toggle-right pattern, 14px bold titles, tight 12px descriptions, 12px row padding, subtle hairlines between rows, and byte-identical markup except for the content strings. The "no structure + ugly typography" feedback is resolved — the rows now have clear list-row structure via the hairlines + right-aligned control column, and the typography weight pair is moderated from 16px-bold-vs-12px-muted down to 14px-bold-vs-12px-muted with tighter line-height. Cross-screen consistency maintained. Net element change per file: 0 elements added, 1 wrapper deleted (the `space-y-3` flex wrapper introduced in 13.5), 3 label elements with class adjustments (justify-start→justify-between, +py-3, +border-b on first two, -p-0 dropped, label-text→text-sm/text-xs). `grep 'style="'` on both files returns zero. `grep 'label-text\|toggle-sm' public/wireframes/account-buyer.html public/wireframes/account-dealer.html` returns zero matches in the notifications sections (no lingering DaisyUI label-text classes, no small-toggle residue). Name/phone edit discoverability question from earlier in the conversation is STILL open — no profile-section changes in this pass, waiting on user's pick between Option A (pencil icons), Option B (dotted underline + Change phone link), or Option C (revert to single Edit button).
- 2026-04-11 — PRODUCT-RULE HOTFIX. account-dealer booth defaults rip-out — direct user catch while drafting the 14. account-dealer handoff prompt: "Wait before we move on i just realize that the dealer account page is fucked up. We go rid of all these location things. Theres just booth number. Please look over everyhting and make sure all these landmarks and last booth number and default row things are gone." Violation of a standing product rule: the only booth-location data on the app is the booth number itself, set per-market in `sell-booth-setup.html`, not as an account-level default. The `account-dealer.html` file contained a full Booth Defaults section with Default Row ("Row C — Antiques"), Landmark ("Across from food trucks"), Last Booth # ("42"), and an explainer "These carry forward to every new market. Override per-market during setup." — all of which are pre-booth-rule Phase 0 content that was authored before the booth-number-only rule was established verbally. Root cause: same failure mode as the T11 hotfix three commits back (3abc6bd) — `account-dealer.html` has never had a dedicated Phase 1 revision pass, so pre-rule content persists in the file and only gets surfaced when the user happens to look at the screen during drive-by verification. Unlike T11 there was no codified `feedback_*.md` memory file for the booth-number rule, so the drive-by audit grep wouldn't have caught it (the audit grep is T11-specific). Hotfix scope: the entire Booth Defaults section ONLY, no other elements touched. Specific changes: (a) deleted the full `<section class="px-4 py-5">` wrapper containing the section label with its `btn btn-xs btn-ghost` Edit button, the `<div class="card card-compact bg-base-200">` outer container, the `card-body` inner container, three `flex items-start justify-between` row blocks (Default Row / Landmark / Last Booth #), and the `text-[10px] text-base-content/60 mt-2 leading-relaxed` explainer paragraph; (b) deleted the trailing `<div class="divider mx-4 my-0"></div>` that separated Booth Defaults from Payment Methods — kept the leading divider (which previously separated Stats from Booth Defaults) so the new rhythm is Stats → divider → Payment Methods, maintaining the canonical one-divider-between-sections pattern used across 8+ wireframes; (c) added a three-line HTML comment in the deletion spot: `<!-- Booth defaults intentionally omitted. Per product rule, the only booth-location data is --><!-- the booth number itself, set per-market in sell-booth-setup, not as an account-level --><!-- default. Do NOT reintroduce "Default Row", "Landmark", or "Last Booth #" fields. -->` — inline markup-level enforcement so a future Claude session editing this file doesn't reintroduce the section after reading the page and thinking "this needs a booth-location configuration area". Net element change: 1 section + ~10 child elements deleted, 0 elements added (the comment block is HTML comments, not elements), 1 divider deleted. CLAUDE.md "no new HTML elements" rule: this is a content-deletion driven by a standing product rule, same class as the T11 hotfix — the rule takes precedence over the wireframe-preservation rule, and the comment block is the minimum addition required to make the rule self-documenting in the markup. `grep -n 'Default Row\|Landmark\|Last Booth\|Booth Default\|Row [A-Z] —\|Across from\|food truck' public/wireframes/` returns only the single line from the new enforcement comment on account-dealer (no actual booth-default data anywhere else in the project). `grep 'style="'` on the file returns zero. Visual verification via preview_screenshot at mobile preset (420px) after forcing a hard navigation to bust the preview browser's cached stale copy confirms: the page now flows Header → Profile → Business Name → Stats → divider → Payment Methods → divider → Notifications → divider → Footer, with no Booth Defaults gap, and the Payment Methods section sits directly under the Stats box separated by the single preserved divider. Notifications section and Edit button on the Profile row are UNTOUCHED in this commit — the cross-screen pill-with-pencil pattern lands in the next commit, separately, so the booth-defaults hotfix has clean isolated git history. Adjacent observation: the pre-T11/pre-booth-rule pattern on `account-dealer.html` implies there may be OTHER unrevised wireframes carrying legacy Phase 0 content that only surfaces during drive-by verification — the pattern is now "dealer-side screens without a dedicated Phase 1 revision pass may contain pre-rule content". The 14. account-dealer dedicated pass (which was the next task at the moment of this hotfix) is still deferred and should audit the entire file against all current product rules, not just T11 and booth-defaults. This incident also suggests a new feedback memory file codifying the "only booth-location data is booth number, set per-market" rule would be worth adding so future drive-by audit greps can catch it the same way T11 is caught.
- 2026-04-11 — 14. Cross-screen edit-pill affordance (account-buyer + account-dealer, authorized cross-cut per established pattern). Resolves the name/phone edit discoverability question raised in 13.7 (Options A/B/C were presented to the user and left open): user picked a variant of Option A with specific direction "the edit button on the top of the dealer page and the (non existent) edit button on the on the buyer account should be subtle. maybe a pill with a pencil on the rigth". Two problems addressed simultaneously with the same pattern: (1) account-buyer profile section had a T9 `input-ghost` invisible-input field for the name with NO visible edit affordance — user explicitly flagged this back in 13.7 ("If i was a user and i needed to edit my name or my number how would i do it?"), and (2) account-dealer profile section had a `btn btn-sm btn-outline Edit` button that was the visually heaviest affordance on the profile row, competing with the user's name for weight — the outline treatment on a settings page read as a primary action rather than a subtle modifier. User's proposed treatment: a single pill-with-pencil design that works on both screens as a subtle modifier for the profile row, making the edit affordance discoverable without dominating. Applied cross-screen to both `account-buyer.html` and `account-dealer.html` (same pattern reasoning as 13.5 and 13.8 — single-screen polish produces outliers; coordinated pattern update maintains cross-screen consistency; coordinated cross-screen changes are authorized when the alternative is inconsistency). Pill markup byte-identical on both screens: `<button class="btn btn-ghost btn-xs rounded-full gap-1 text-base-content/60">Edit ✎</button>` — `btn-ghost` for transparent-until-hover treatment (subtle, no chrome), `btn-xs` for the smallest DaisyUI button size (shorter than the original `btn-sm` outline), `rounded-full` to convert the default rectangle into a true pill shape, `gap-1` to give 4px breathing between the "Edit" text and the pencil glyph, `text-base-content/60` to pull the color down to 60% opacity so the button reads as supporting content rather than primary action. Label "Edit ✎" — text + Unicode pencil U+270E (lower right pencil) on the right edge of the pill, matching the user's "pencil on the right" description. Unicode pencil chosen over an SVG icon because the project has no icon library and JetBrains Mono gives the character consistent width and rendering; the `gap-1` class handles spacing between the text and the glyph. Per-screen changes: **account-dealer.html** — replaced `<button class="btn btn-sm btn-outline">Edit</button>` on the profile row with the new pill markup; same slot, same position (right-aligned after the `flex-1` name/phone block), just different classes + glyph; added a two-line HTML comment above the button noting the cross-screen pattern and that it replaces the earlier outline treatment. **account-buyer.html** — the T9 input-ghost treatment on the name field (`<input type="text" value="John Chen" class="input input-ghost font-bold text-lg w-full px-0 h-auto min-h-0 py-0 focus:outline-none" />`) was deleted entirely and replaced with a static `<div class="font-bold text-lg">John Chen</div>` that matches the dealer-side profile treatment exactly — the pill button is now the explicit edit affordance, so the input-ghost invisible-field pattern is no longer needed (and was the failed UX the user flagged in 13.7); added the pill button in the same slot as the dealer (right-aligned after the `flex-1` block) for visual parity; added a two-line HTML comment above the button noting the cross-screen pattern and that it supersedes the T9 input-ghost approach. T9 theme (inline edit via input-ghost) is now effectively retired on the account screens — the explicit edit-pill pattern replaces it. Net element change per file: account-dealer: 1 button class/label change (outline → pill), 2 HTML comments added; account-buyer: 1 input deleted, 1 div added, 1 button added, 2 HTML comments added. CLAUDE.md "no new HTML elements" rule: the buyer-side button addition is authorized by the user's direct directive ("the (non existent) edit button on the on the buyer account should be subtle") — the user is explicitly asking for it, not inventing it. `grep 'style="'` on both files returns zero. `grep 'btn btn-sm btn-outline\|input-ghost' public/wireframes/account-buyer.html public/wireframes/account-dealer.html` returns zero matches in the profile sections after this pass (no lingering outline button, no lingering input-ghost invisible field). T11 drive-by audit on account-dealer.html (required per `feedback_eb_no_payment_middleman.md` whenever a dealer-side screen is visited): grep `@\|venmo\|zelle\|paypal\|btn.*Connect\|btn.*Edit.*payment\|Not connected\|handle` returns only CDN URL fragments (`@300;400;...`, `@4.12.23`) and method-label text (Venmo/Zelle as method labels inside font-bold divs, not handles) — T11 compliance intact, no pre-T11 markup resurfaced during this pass. Visual verification via preview_screenshot at mobile preset (420px) on both `account-dealer.html` and `account-buyer.html` after forcing a hard navigation (the preview MCP tool's headless browser had cached the pre-edit content and was ignoring path/query-param cache-busts — `window.location.href = ... + Date.now()` was needed to force an actual fresh fetch): the profile row on both screens now reads `[avatar] [name / phone] [subtle Edit ✎ pill]` with the pill visually subordinate to the name block (ghost treatment blends into the page background until hover, the pencil glyph is small, 60% opacity pulls it toward the page background), and the two screens render byte-identical profile markup except for content strings (Marco Ferraro/MF vs John Chen/JC, different phone numbers). Cross-screen consistency locked in. Adjacent consideration: the "edit" affordances on OTHER sections were NOT updated in this pass — account-dealer's Business Name section has an `input input-bordered` which is already editable inline so no edit button is needed there, and account-buyer has no other sections with editable content. Scope is tightly the profile-row edit affordance only. Discoverability question from 13.7 is now RESOLVED. The 14. account-dealer dedicated Phase 1 revision pass (all sections, not just profile) is still deferred and should audit the entire file against the full T1-T13 theme set.
- 2026-04-11 — 14. account-dealer DEDICATED PHASE 1 REVISION PASS (first full-file audit). Prior to this session the file had received only targeted hotfixes and cross-cuts (T11 payment-handles rip-out 3abc6bd, booth-defaults rip-out ff13060, 13.5/13.8 notifications restructures, 14.x edit-pill cross-screen pass 493f80c) but had never had its own dedicated walk-through against the full T1-T13 theme set, so this session treated every section as in-scope per the user's explicit instructions ("Treat every section (header, profile, business name, stats, payment methods, notifications, footer, bottom nav) as in-scope"). T11 drive-by audit grep run on entry per `feedback_eb_no_payment_middleman.md`: `@\|venmo\|zelle\|paypal\|btn.*Connect\|btn.*Edit.*payment\|Not connected\|handle` returned only benign matches (CDN URLs, the existing T11 enforcement comment, "Venmo"/"Zelle" as method labels inside font-bold divs). T7 grep `pre-buy|preview|pre-market` returned zero matches. T5 grep for `State \d|dealer.*state|buyer.*state` returned zero matches. Pre-audit confirmed clean state on T5/T7/T11 — no hotfix needed before primary work. Four primary edits applied this pass: **(a) Header canonical-pattern rebuild (T5/T8 + cross-screen symmetry with account-buyer):** the header right slot had a `text-xs uppercase tracking-widest text-base-content/60` "Account · Dealer" eyebrow div which (i) was a role-context label rather than the canonical header content, and (ii) didn't match the established tab-screen header pattern locked in by sell-add-item pass 6 + account-buyer pass 1 (one text-lg text element on the left + 32px avatar circle on the right). The eyebrow was deleted and replaced with `<div class="avatar placeholder"><div class="bg-neutral text-neutral-content w-8 rounded-full"><span class="text-xs font-bold">MF</span></div></div>` — byte-identical to account-buyer's header avatar slot except for the "MF" initials (vs "JC" on the buyer side). Left slot promoted from a static `<div class="text-lg font-bold tracking-tight">EARLY BIRD</div>` to `<a href="home-dealer.html" class="text-lg font-bold tracking-tight">EARLY BIRD</a>` (T8 — dealer's logo routes to home-dealer lobby, mirrors account-buyer routing to home-buyer). The 32px header avatar is intentionally a duplicate of the larger w-16 in-page profile avatar below it; the duplication is acceptable because the header avatar is a structural nav element that exists on every tab screen for cross-screen consistency, while the in-page avatar is profile content. **(b) Stats block deletion (note 14.3):** the entire `<section class="px-4 pb-4">` containing the `stats stats-horizontal bg-base-200 w-full` block with three stats (Listed: 18 / Sold: 142 / Markets: 9) was deleted. Per the per-screen note: "Drop the metrics / stats cards. They don't belong on the account screen." Replaced with a 4-line HTML comment explaining the omission, why it's intentional (Account = identity + settings, not performance reporting; metrics belong on the dealer's Sell tab dashboard), and explicitly noting the asymmetry with account-buyer (which keeps its Watching/Inquiries/Bought stats block) — the buyer-side stats weren't flagged for removal in the per-screen review notes, so the asymmetry is by design and a future editor reading this file shouldn't "fix" the asymmetry by adding stats back. The asymmetry is grounded in role: dealer Sell tab will own the dealer's performance reporting (a real working surface), whereas the buyer has no equivalent operational tab — the buyer's stat block is the only place those metrics live, so removing them would lose the data entirely. **(c) Bottom nav T10 panel treatment:** `<nav class="max-w-md mx-auto fixed bottom-0 left-0 right-0 bg-base-100 border-t border-base-300 flex">` → `<nav class="max-w-md mx-auto fixed bottom-0 left-0 right-0 bg-base-200 flex">` — drops the hard `border-t` line-above indicator and switches the nav background to `bg-base-200`, matching the convention 04/05/06/07/08/09/10/11/13 all converged on. The vertical `<div class="w-px bg-base-300 my-2"></div>` divider between Watching and Sell is preserved (base-300 still reads against base-200 background). The active-tab `border-t-2 border-neutral` indicator on the Account link is preserved — that's a per-tab active state, not the nav-wide line that T10 prohibits. **(d) Footer-redesign TODO mirror from account-buyer:** the `<!-- Footer links -->` section header gained a 2-line TODO comment matching the one already in account-buyer.html — flagging the entire Help/Sales/Privacy/Terms + Sign Out + version stack for the deferred follow-up redesign session. The follow-up checklist item in EB_DESIGN.md ("Account-screen footer redesign") explicitly says "Likely also applies to `account-dealer.html` if it carries the same footer structure (don't peek inside the 13 session; verify when this follow-up runs)" — confirmed during this pass that the dealer footer structure is byte-identical to the buyer footer (4 link link-hover anchors, divider my-2, btn btn-sm btn-ghost text-error Sign Out, text-[10px] text-base-content/40 version line) except for "Dealer Help"/"Sales History" being dealer-specific link labels and the version line including "· Dealer Account". Mirroring the TODO ensures the follow-up session can grep both files for the same marker and treat them as a coordinated cross-cut. The footer's content/styling is NOT touched in this pass — the deferral is honored and the Sign Out red treatment, the link link-hover muted-until-hover treatment, the divider rhythm, and the arbitrary `text-[10px]` version line all remain in place pending the dedicated redesign session. **Theme-by-theme N/A breakdown for this pass:** T1 N/A — no sticky CTA stacked above the bottom nav (the nav is the only fixed-bottom element, no gap glitch possible). T2/T3 N/A — no badges or pills exist on this screen at all after the stats deletion (payment section uses checkboxes; notifications use toggles + hairline rows; business name is an input; footer links are `link link-hover` text not pills). T4 N/A — no back/close button in page chrome (top-level Account tab, not a sub-screen). T5 — the "Account · Dealer" eyebrow wasn't a `State N` dev string, but the canonical header rebuild removed it anyway as a side effect of the symmetry-with-buyer fix. T6 N/A — no filter row on this screen. T7 N/A — grep clean, no pre-buy/preview language anywhere in the file. T8 — addressed in (a) via the EARLY BIRD `<a href="home-dealer.html">` wrap. T9 — already retired on the account screens by 14.x's edit-pill cross-screen pass (493f80c); the explicit edit-pill pattern superseded the input-ghost invisible-field treatment that T9 originally specced, and the profile row already has the pill in place. The Business Name section retains its `input input-bordered` treatment as a legitimate form field — the edit-pill pattern only applies to identity displays (profile name), not to standalone settings inputs. T10 — addressed in (c) via the bg-base-200 nav panel switch. T11 — already enforced by the 3abc6bd hotfix; pre-pass drive-by audit grep returned no violations; verified again post-edits. T12 N/A — no modals/drawers on this screen. T13 N/A — no LIVE pill or market status indicator on this screen. **Cross-screen symmetry checks against account-buyer (per the user's instructions):** (i) header structure now byte-identical except for the linked target (`home-dealer.html` vs `home-buyer.html`) and the avatar initials (`MF` vs `JC`), (ii) profile-row edit-pill markup byte-identical (already done in 14.x cross-cut), (iii) bottom nav `bg-base-200` panel now matches (was previously asymmetric — buyer was already on bg-base-200, dealer was still on bg-base-100 border-t), (iv) footer-redesign TODO comment markup byte-identical (header changed to "Help/Sales/Privacy/Terms" to reflect the dealer's link list having a "Sales History" entry not present on buyer), (v) Notifications section structure already byte-identical from the 13.8 cross-screen pass. The asymmetries that remain by design: (a) dealer keeps the Business Name section, buyer doesn't (dealer-only field), (b) dealer keeps the Payment Methods section, buyer doesn't (dealer-only concern), (c) buyer keeps the Stats block, dealer drops it (per note 14.3, see (b) above for the asymmetry rationale), (d) buyer has the Markets You Follow + Become A Seller sections, dealer doesn't (buyer-only concerns — dealers don't follow markets the same way and don't need an upgrade flow), (e) bottom nav is 4 tabs (Buy/Watching/Sell/Account) on dealer vs 3 tabs (Buy/Watching/Account) on buyer per the canonical role split in EB_DESIGN.md "Navigation". **OQ4 (note 14.4 — booth defaults per-market vs global):** flagged but NOT acted on in this pass per the protocol rule "Do not try to answer them inside a revision session — flag them in the commit message". The current state of the file post-booth-defaults-rip-out (ff13060) has NO booth-defaults section at all (the rule that emerged in that hotfix was "the only booth-location data is the booth number itself, set per-market in sell-booth-setup, not as an account-level default"), which means OQ4 is effectively answered for v1: there are no booth defaults to be either global or per-market because there are no booth-default fields at all. The original OQ4 framing assumed booth defaults existed; the booth-defaults rip-out collapsed the question. Surfacing this collapse in the commit message so a future product session can resolve OQ4 by either (i) accepting that "no booth defaults at all" is the answer and closing the OQ, or (ii) re-introducing some account-level field (which would conflict with the established rule). **Things explicitly NOT touched this pass:** (i) the profile section (avatar/name/phone/Edit pill — already canonical from the 14.x cross-cut, byte-identical to account-buyer), (ii) the Business Name input (legitimate form field, not flagged in any review note, retains `input input-bordered` treatment as the canonical settings-input primitive), (iii) the Payment Methods section (T11-compliant since 3abc6bd, no further work needed), (iv) the Notifications section (canonical 13.8 pattern with label-left/toggle-right + hairlines + py-3 rows, byte-identical to account-buyer), (v) the Footer content (Sign Out red treatment, link list, version line — all deferred to the open follow-up redesign session per the mirrored TODO comment), (vi) the four `divider mx-4 my-0` between-section dividers (canonical project pattern across 8+ wireframes), (vii) section padding and rhythm (`pt-6 pb-4`, `pb-4`, `py-5`, `py-5 pb-8` — preserved exactly as the canonical post-identity rhythm matching account-buyer). Net element change: 6 elements deleted (3 stats children + their wrapper section + the eyebrow div + the duplicate profile-section div → wait, no, recount: 1 eyebrow div deleted, 1 stats section deleted with its 3 stat children = 4 elements + 1 wrapper, 1 div promoted to anchor (no element added), 4 elements added (1 anchor wrapper for EARLY BIRD logo treatment, 1 avatar div + 1 inner div + 1 span for header avatar). Actual net: deletions and additions roughly balance — the structural change is moving the right-side header content from a single text div to a 3-element avatar nest, which is the same pattern account-buyer uses. CLAUDE.md "no new HTML elements" rule: the avatar nest replaces an existing right-side div, the anchor wrap is a tag change on an existing element, the stats deletion is pure removal, and the footer TODO is HTML comments. No new sections or wrappers added beyond what's needed to match the canonical header structure that already exists on account-buyer/sell-booth-active/sell-add-item. Justified by the user's explicit "Treat every section as in-scope" + the cross-screen symmetry instruction. Visual verification via preview_screenshot at mobile preset (375x812) on the live wireframe: top-of-page screenshot confirms header now reads `EARLY BIRD | [MF avatar circle]` matching account-buyer exactly, profile row reads `[MF avatar] [Marco Ferraro / phone] [Edit ✎ pill]` with the pill subordinate to the name block, Business Name input below profile, divider, Payment Methods section header + explainer + 4 checkbox rows with hairlines, divider, Notifications section starting; bottom-of-page screenshot confirms the three notifications rows render with the canonical 13.8 pattern (label-left/toggle-right + hairlines + py-3), the deferred footer renders with its known-ghastly Help/Privacy/Terms link list + red Sign Out button + version line (preserved per the TODO), and the bottom nav now reads as a unified `bg-base-200` panel with no `border-t` line above it. preview_eval DOM inspection confirmed: `headerLogo: home-dealer.html`, `headerAvatarText: MF`, `statsExists: false`, `navBgClass: max-w-md mx-auto fixed bottom-0 left-0 right-0 bg-base-200 flex`. `grep 'style="'` on the file returns zero. Post-edits T11 drive-by audit grep returns zero violations. Notes addressed: 14.1 (already done in 14.x cross-cut, verified), 14.2 (already done in 3abc6bd hotfix, verified), 14.3 (stats deleted), 14.4 (flagged in commit message, not acted on per protocol — and the booth-defaults rip-out has effectively collapsed the OQ to "no defaults at all"), 14.5 (T8 logo route to home-dealer added). Cross-cutting themes addressed: T5 (header eyebrow removed as side effect of canonical-header rebuild), T8 (logo wrap), T10 (nav panel). N/A this pass: T1, T2, T3, T4, T6, T7, T11, T12, T13 (all listed above with reasons). **Mid-session typographic fix on Payment Methods explainer (driven by direct user feedback after initial preview):** user flagged two issues on the live screenshot — "the spacing between payment methods and the below text feeels too big. Also the text is spanning too great of a horizontal width." Two targeted edits to the explainer block: (i) section label `text-xs uppercase tracking-widest text-base-content/60 mb-3` → same classes with `mb-3` → `mb-1` (12px → 4px gap between label and explainer, tightening the label-to-explainer relationship so they read as a single labeled-content unit rather than two free-floating elements), (ii) explainer div added `max-w-[16rem]` constraint (256px). The 16rem value was chosen by measuring the actual rendered width of the longest target line via preview_eval injecting a hidden probe span: "Payment happens in person at the booth." renders at 234px in JetBrains Mono at 10px font-size, "Buyers see which methods you accept." renders at 216px, so 256px gives 22px slack on the longer line and 40px slack on the shorter — both lines fit comfortably with no risk of sub-pixel wrap edge cases. First attempt at `max-w-[14rem]` (224px) wrapped the second line into 3 lines with an orphaned "booth." on its own line; bumped to 16rem to get exactly 2 lines breaking naturally at the period. DOM verification post-fix: explainer width=256px, height=32.5px (2 lines at 16.25px line-height confirmed). Visual verification: the explainer now reads as a tight 2-line supporting-copy block sitting directly under the PAYMENT METHODS label, with the right edge of the text well inside the container width — supporting copy is visually subordinate to the main payment-method rows below it. The `mb-3` on the explainer (gap to the first checkbox row) is preserved — only the label-to-explainer gap was tightened, not the explainer-to-content gap.
- 2026-04-11 — 14. Payment-methods explainer width follow-up (`max-w-[16rem]` → `max-w-[22rem]`, commit aa2e06d). After 0f43d81 shipped with the explainer constrained to 16rem (256px), user pushed back: "you wrapped the text too early." The 16rem value broke the explainer at the sentence boundary ("Buyers see which methods you accept." / "Payment happens in person at the booth.") which read as too aggressive — left ~160px of right-side whitespace inside the column, making the supporting copy feel cramped to one side rather than naturally narrow. The original problem (unconstrained ~416px width "spanning too great of a horizontal width") and the over-correction (16rem clean-sentence-break with too much whitespace) bracket the acceptable range; the middle-ground value picked here is 22rem (352px). At 22rem the wrap lands mid-second-sentence after "happens" — line 1 reads "Buyers see which methods you accept. Payment happens" and line 2 reads "in person at the booth." — which is asymmetric at the line level but consumes ~82% of the available column width (343px actual rendered width inside a 416px container) instead of the 16rem run's ~62% (256px / 416px). The tradeoff: 16rem produces cleaner per-line semantics (each sentence on its own line) but feels narrow; 22rem produces a more comfortable visual width but breaks mid-sentence. User feedback is the deciding vote and they wanted the wrap pushed later, so 22rem wins. The `mb-1` label-to-explainer tightening from the prior commit is preserved — only the `max-w-[16rem]` token was changed to `max-w-[22rem]`. Single-token edit, no structural change. preview_eval DOM verification: `width: 343, maxWidth: '352px', lineCount: 2`. preview_screenshot at 375x812 confirms the explainer now reads as a fuller-width 2-line supporting-copy block under the PAYMENT METHODS label without the right-side dead zone of the 16rem version. Note for future revision sessions: the per-screen review notes for screen 14 didn't specify a target width or wrap behavior for this explainer — the constraint exists only because of the user's mid-session feedback flow ("too wide" → 16rem → "too early" → 22rem), so a future audit shouldn't read the `max-w-[22rem]` as an established pattern to propagate elsewhere. It's specific to this explainer's content length (74 chars) and JetBrains Mono's character width at 10px font-size. Other supporting-copy blocks elsewhere in the project should be sized to their own content, not to 22rem.
- 2026-04-11 — 12. sell-market-picker DELETION pass — executed the deletion specced in PHASE_1_REVIEW_NOTES.md notes 12.1–12.2: dealers won't be focused on a market two weeks out, and the home-dealer lobby (T8, note 09.2) absorbs the market-selection function so a dedicated picker screen is dead weight. Six edits in one commit, no other surfaces touched. (a) `public/wireframes/sell-market-picker.html` deleted (198-line file, removed via `rm`). Wireframes folder count went 15 → 14, verified by `ls public/wireframes/ | wc -l`. (b) `public/review.html` four updates: the `WIREFRAMES` array entry `'sell-market-picker',` removed; the sidebar count `Wireframes (15)` → `Wireframes (14)`; the default progress text `1 / 15` → `1 / 14`; the clear-all confirm copy `Clear ALL notes across all 15 wireframes?` → `Clear ALL notes across all 14 wireframes?`. The progress text auto-updates from `WIREFRAMES.length` on first render so the hardcoded `1 / 15` only affects the very first paint before JS runs, but updating it keeps the source consistent with the array length. (c) `EB_DESIGN.md` Sell Tab section: the `**Market Picker:** Switch between upcoming markets...` bullet on line 140 deleted entirely. Considered adding a one-line "market switching lives on home-dealer" transition note in its place — drafted it, then deleted per the project's "no removed-comments hacks" rule (CLAUDE.md user-feedback memory: "If you are certain that something is unused, you can delete it completely"). The home-dealer section above already says where market switching lives, so a duplicate transition note in the Sell Tab section is just bloat. (d) `EB_DESIGN.md` Phase 1 file list (line ~248): `- sell-market-picker.html` removed from the wireframes inventory under "Phase 1: Wireframes". (e) `EB_DESIGN.md` Phase 1 Revision Workflow → Special cases: the `**"Remove this page"** (applies to sell-market-picker, notes 12.1–12.2)` bullet on line ~311 deleted entirely. The bullet existed to brief the session that would handle this checklist item; once the deletion lands the briefing is dead documentation, same logic as removing the bullet from "The Screens". (f) `EB_DESIGN.md` Home Lobby (Dealer) paragraph on line ~106: the trailing clause `Market switching happens here too — the dealer picks which market to set up for from this lobby instead of from a separate market-picker screen.` rewrote to `Market switching also happens here — the dealer picks which market to set up for from this lobby.` — drops the "instead of from a separate market-picker screen" comparison since the screen no longer exists for the comparison to land against. The positive statement (market switching happens here) survives because it's the canonical doc-level statement of where market-switching lives in the new architecture. (g) `EB_DESIGN.md` checklist line 334: flipped `[ ] 12. sell-market-picker` → `[x] 12. sell-market-picker`. Verification: `grep -E 'sell-market-picker|[Mm]arket [Pp]icker|market-picker' public/wireframes/ public/review.html EB_DESIGN.md CLAUDE.md` returns the single intentional remaining hit on the checklist line (`[x] 12. sell-market-picker`). PHASE_1_REVIEW_NOTES.md and REVISION_LOG.md were intentionally NOT touched: the former is the spec source for this deletion (notes 12.1–12.6 are the canonical record of WHY this happened, removing them would erase the rationale); the latter is the historical log including older entries that mention `sell-market-picker.html` in cross-screen context (T13 deferral list, account-buyer revert post-mortem) — historical references stay historical. The user's instruction explicitly scoped the grep cleanup to "wireframes + EB_DESIGN.md + CLAUDE.md", which excludes both files. CLAUDE.md had zero references — no edit needed. Other wireframe files had zero references — no edits needed (no nav links, no comments, no anchors). The `_scratch/` exploration folder also had zero references (verified via the project-wide grep before deletion). No code in `src/` referenced the wireframe (Phase 2/3/4 hasn't begun). T13 deferred-list note in PHASE_1_REVIEW_NOTES.md line 117 still mentions sell-market-picker as a current LIVE-pill instance — that list is now stale by one entry but PHASE_1_REVIEW_NOTES.md is out-of-scope for this session, and the future T13 cross-screen pass will rediscover the actual instances by grepping `badge-success` across `public/wireframes/` (which is what the note tells the future session to do anyway). Single commit per the protocol; checklist flipped; ends.
- 2026-04-11 — home-buyer NEW SCREEN pass — built `public/wireframes/home-buyer.html` from scratch per note 04.3 + EB_DESIGN.md "Home Lobby (Buyer)" spec. This is a brand-new file, not a revision: the buyer's logged-in lobby that sits ABOVE Buy/Watching/Account in the post-login flow (landing → magic link → onboarding → home-buyer → tabs). Built in a dedicated session with its own clean context per the "we need a new screen" protocol carve-out — buy-feed pass 04 explicitly did NOT touch this file when it surfaced the need; that session only added the doc-level T8 wiring + the checklist item. Three sister wireframes (`account-buyer.html`, `watching.html`, `buy-feed.html`) had already wired their EARLY BIRD wordmark to `home-buyer.html` in anticipation, so the canonical filename was locked in before this session started — verified via `grep home-buyer.html public/wireframes/` before writing. **Cross-screen reads logged per protocol:** read `watching.html` and `account-buyer.html` (the two sister wireframes named in the handoff prompt) for the canonical header + bottom-nav patterns; did NOT read any other wireframe files to avoid cross-screen context pollution. **Spec sources read this session:** `CLAUDE.md` (global rules — no inline styles, mono everywhere, no new HTML elements beyond what the screen needs), `EB_DESIGN.md` "Phase 1 Revision Workflow" section + "Home Lobby (Buyer)" subsection (lines ~100-103 — the canonical spec for this screen), `PHASE_1_REVIEW_NOTES.md` cross-cutting themes T1-T13 (the hidden half of every screen's spec). Did NOT read any other per-screen sections. **Layout (top to bottom):** (a) Header — EARLY BIRD wordmark on the left + 32px JC avatar circle on the right, byte-identical structure to account-buyer's header except the wordmark is wrapped in a `<div>` instead of an `<a>`. The wordmark is intentionally NOT a link on this screen because this IS the home lobby — every other post-login screen routes to `home-buyer.html` per T8, but a self-route from home to home would be a no-op, so a plain div is more honest than a self-link. HTML comment above the header explains this so a future editor doesn't "fix" it by adding the anchor wrap back. (b) Live Now section — uppercase eyebrow `text-xs uppercase tracking-widest text-base-content/60 mb-3` matching the section-header convention from account-buyer (Notifications / Markets You Follow / Become A Seller all use the same eyebrow). One market row beneath: Downtown Modernism · Sat Apr 26 with `badge badge-success badge-sm font-bold` LIVE pill on the right, `32 dealers · 487 items` count line, and a bottom row with `Live now · Ends 8:00pm Sat` on the left + `Shop the drop →` CTA text on the right. Row uses `block px-4 py-4 border-y border-base-300` so the row itself is the tap target (links to buy-feed in Phase 3). The LIVE pill uses the existing `badge badge-success` pattern per T13's deferral note ("Deferred to a cross-screen pass — do NOT apply [the lightning-bolt circle] inside a single-screen revision session"). T13 will replace it across every screen at once in the dedicated cross-screen pass — using the new pattern here would put this screen out of step with the other 6 screens that still carry the old pattern. (c) Upcoming section — same eyebrow primitive, two market rows (Rose Bowl Flea · Sun May 11 / PCC Flea · Sun May 18), each showing market name + `~80 dealers` count + `Drops in Nd Nh` countdown headline + `Sat May 10 · 6:00pm` exact-time supporting line in `text-[10px] text-base-content/60 uppercase tracking-wider`. Intentionally NO `UPCOMING` pill on these rows — T2 says outline-down by default and the absence of a LIVE badge already conveys upcoming; adding a "UPCOMING" pill would just be visual noise. HTML comment above the section explains this so it doesn't get added back. The countdown headline uses `text-sm font-bold` (the FOMO-driving info gets the most weight); the exact drop time below is the supporting detail. (d) Drop Alerts toggle — single `form-control` row using the canonical `label.label cursor-pointer justify-between p-0 py-3 gap-4` pattern from account-buyer's Notifications section, with `text-sm font-bold` label "Drop Alerts" + `text-xs text-base-content/60` subtitle "Text me when a market I follow goes live" + `input type="checkbox" checked class="toggle"` on the right. Single row, no border-bottom hairline (this is a standalone toggle, not a stack of rows like account-buyer's Notifications section). HTML comment notes that per-market notification preferences live on Account → Notifications, not here — this is the single global opt-in. (e) FAQ — DaisyUI `collapse collapse-arrow border border-base-300` accordion with 4 entries: "How does Early Bird work?", "What's a 'drop'?", "How do I contact a dealer?", "How do I pay?". Each entry has a `text-sm font-bold` collapse-title + `text-xs text-base-content/70 leading-relaxed` collapse-content paragraph. Pure HTML/CSS accordion (DaisyUI primitive), no JS needed for the wireframe. The "How do I pay?" answer aligns with T11/the EB_DESIGN.md payment-handles policy: "You pay the dealer in person at the booth — cash, Venmo, Zelle, Apple Pay, or card, whichever the dealer accepts. Early Bird never handles payment." Kept the FAQ short (4 entries) per the spec — longer help/policy content lives in Help & Support (account screen footer link). (f) Bottom nav — three tabs (Buy / Watching (7) / Account) using the canonical `max-w-md mx-auto fixed bottom-0 left-0 right-0 bg-base-200 flex` panel treatment. T10 compliant: `bg-base-200` background, NO `border-t border-base-300` line-above. The vertical `<div class="w-px bg-base-300 my-2"></div>` divider between Watching and Account is preserved (3-tab buyer nav puts the divider between Watching and Account, mirroring watching.html and account-buyer.html). **None of the three tabs has an active state** — no `border-t-2 border-neutral` indicator, all three links use `text-base-content/60` (the inactive color). This is correct because the home lobby sits ABOVE the tabs in the navigation hierarchy: you're not "on" any of the tabs when you're at home. HTML comment above the nav explains this so it doesn't get "fixed" by adding an active state. **Theme-by-theme breakdown for this pass:** T1 N/A — no sticky bottom CTA stacked above the nav (the nav is the only fixed-bottom element, no gap glitch possible). The `pb-24` on `<main>` provides clearance under the fixed nav so the FAQ's last entry doesn't get hidden behind it. T2 — addressed: only the LIVE badge is filled (`badge-success`, the one endorsed alerting state); no UPCOMING pill on upcoming rows; no other badges anywhere. T3 N/A this session — the LIVE badge uses default `badge-sm` which is the existing project-wide pattern; T13 will replace it with the icon-circle treatment in the cross-screen pass and that pass owns the padding question (an icon circle is a different padding model than a text pill). Adding `px-2 py-2` to the soon-to-be-replaced text pill would be wasted churn. T4 N/A — no back/close button (top-level home lobby, not a sub-screen). T5 N/A — no `State N` or role-name labels in the header (header is just the canonical wordmark + avatar). T6 N/A — no filter row on this screen. T7 — addressed: copy uses "Shop the drop" framing throughout. Grep for `pre-buy|preview|pre-market` on the new file returns zero. The "Shop the drop →" CTA on the live market row + the FAQ entry about drops both use the correct shopping-marketplace framing. T8 — this IS the home lobby, so the screen is the destination of T8's logo-routing rule rather than a screen that needs the rule applied to it. The wordmark is intentionally a `<div>` not an `<a>` for the reason explained above. T9 N/A — not an account screen, no profile fields to edit. T10 — addressed: nav uses `bg-base-200` panel treatment, no `border-t` line above. T11 — N/A directly (no payment fields on this screen) but the "How do I pay?" FAQ answer is T11-compliant: explicitly says Early Bird never handles payment, payment happens at the booth, no stored handles implied. T12 N/A — no modals/drawers on this screen. The FAQ uses an inline accordion (DaisyUI collapse), not a drawer or modal — taps expand the content in place. T13 — deferred per the explicit scope note ("do NOT apply this inside a single-screen revision session"). The LIVE pill uses the existing `badge-success` pattern; the future cross-screen T13 pass will rediscover it via `grep badge-success public/wireframes/` and update it alongside every other LIVE pill in the app. **Dummy data:** JC (John Chen) avatar, 3 followed markets matching the buyer's follow list from `account-buyer.html` (Downtown Modernism / Rose Bowl Flea / PCC Flea — Melrose Trading Post is intentionally excluded because account-buyer.html shows it as un-followed, so it shouldn't appear on the buyer's home). Watching count `(7)` matches account-buyer's stat block. Drop times use the canonical "evening before the market" rule from EB_DESIGN.md ("usually the evening before the market"). The "29d 6h" / "36d 6h" countdowns are illustrative dummy values for the wireframe — Phase 3 wiring will compute the actual deltas. Downtown Modernism's LIVE state contradicts the Apr 26 future date relative to today's Apr 11 — kept anyway because the wireframe needs to demonstrate what the LIVE state looks like and Downtown Modernism is the canonical "current market" across watching.html / item-detail-buyer.html / etc. The temporal contradiction is invisible in the wireframe (no timestamps shown) and resolves naturally in Phase 3 once the dummy data goes away. **review.html WIREFRAMES update (14 → 15):** four edits to `public/review.html` matching the pattern from the 12. sell-market-picker DELETION pass but in reverse — (i) added `'home-buyer',` to the `WIREFRAMES` array between `'onboarding'` and `'buy-feed'` (position 4), placing it in post-login-flow order so the review tool walks the screens in narrative order rather than alphabetical; (ii) sidebar count `Wireframes (14)` → `Wireframes (15)`; (iii) default progress text `1 / 14` → `1 / 15`; (iv) clear-all confirm copy `Clear ALL notes across all 14 wireframes?` → `Clear ALL notes across all 15 wireframes?`. The progress text auto-updates from `WIREFRAMES.length` on first JS render so the hardcoded `1 / 15` only affects the very first paint, but updating it keeps the source consistent with the array length (same rationale as the deletion pass). **EB_DESIGN.md changes:** ONLY the checklist flip for `home-buyer` (line 333: `[ ]` → `[x]`). No other EB_DESIGN.md edits — the "Home Lobby (Buyer)" spec was already written by the buy-feed pass (04.3) and the Phase 1 file list already includes `home-buyer.html`. CLAUDE.md, PHASE_1_REVIEW_NOTES.md, and the other 14 wireframe files were intentionally NOT touched. **Verification:** `grep 'style="' public/wireframes/home-buyer.html` returns 0; the file uses only DaisyUI classes and Tailwind utilities. Single commit per the protocol; checklist flipped; ends.
